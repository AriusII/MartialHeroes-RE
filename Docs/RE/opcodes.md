# Opcode Catalog (authoritative)

<!--
verification: routing/sizes [confirmed] (opcode->handler ROUTING, frame-header layout, dispatch
  shape, table bases, C2S send sizes, and S2C handler read sizes/offsets are all
  control-flow-confirmed); value-semantics [capture/debugger-pending] (every packet field VALUE
  semantic — what a wire byte MEANS — has no live capture this campaign). CYCLE 14 re-anchor
  (f61f66a9, 2026-06-27): 1 fact re-confirmed SAME (wire frame header + dispatch shape); 2
  corrected (5/59 IDB name lost — catalog name SmsgRankProgressSfxEvent retained, body size
  confirmed 76 bytes; 5/93+5/94 IDB mislabeled — catalog names SmsgActorMobIdChange /
  SmsgVoteResult confirmed by binary debug strings; renames pending, ida-toolsmith).
ida_reverified: 2026-06-24
ida_reverified: 2026-06-27
ida_reverified: 2026-06-30
ida_anchor: f61f66a9ae0ec1e946105b2ecff76e8930cb1d1367df64e5688a5266f5ad9963
confidence: CONSUMER-CONFIRMED
evidence: [static-ida]
capture_verified: false
cycle7: re-verified against doida.exe IDB SHA 263bd994, CYCLE 7 (2026-06-20). Gameplay/dark-subsystem
  relabels folded in (the binary wins): combat melee / use-skill = C2S 2/52 (SkillSlot 0xFF = basic
  melee) — there is NO "5/52" combat opcode (the old "5/52 combat" was a transposition for the 2/52
  use-skill builder); S2C 4/99 + 4/100 = the Cube-Gamble minigame (result panel + reel update),
  RENAMED SmsgCubeGambleResultMessage / SmsgCubeGambleReelUpdate (the "SmsgCombat*" labels are
  DROPPED — combat is server-authoritative via 2/52, no client-side combat-result apply); buff/status
  slot update = S2C 5/31 SmsgBuffSlotUpdate (56B), which SUPERSEDES the older "4/102 buff" label
  (4/102 = SmsgSkillWindowStateUpdate, the skill/state-window snapshot, NOT the buff channel); 5/73 =
  SmsgQuestComplete (the GuildWar reading is REFUTED); 3/23 = SmsgCharStatusBytesByName (28B, by-name
  roster patch — NOT a 12B char-create result); C2S 2/60 = couple/marriage relation request (reply
  5/53), NOT mail/letter (the CmsgLetterRequest autoname is misleading — mail rides 2/70/2/71/4/70);
  the move emitter is C2S 2/13 (16B) — 2/112 is an event-driven keepalive toggle, NOT the move
  heartbeat; S2C 4/70 = the delivery/mail inbox record (140B, reply to 2/71); S2C 4/126 =
  faction/brood-war side assignment. *spec:* handlers.md (§4/99, §4/100, §5/31), net_contracts.md
  (§2.3, §2.10, §2.12b, §2.13).
cycle8: Phase 2.1 reconcile (2026-06-21, IDB 263bd994, static-ida, the binary wins) — two
  packet-spec consistency conflicts settled: (a) 3/6 SmsgRenameCharResult body is **12 bytes**
  (NOT 19; the prior embedded-name reading is refuted — Result@0, ErrorCode@1, pad@0x02, and two
  trailing IEEE-float placement values @0x04/@0x08); (b) 3/23 SmsgCharStatusBytesByName name key
  is at **body offset 0x08** (NOT 0x00/0x02; leading 8-byte block copied-but-unused). *spec:*
  packets/3-6_rename_char_result.yaml, packets/3-23_char_select_status_update.yaml, login_flow.md
  (S5.4, S5.7).
conflicts: resolved this pass (CYCLE 4 Netcode Deep-Cartography promotion, the binary wins) —
  (1) renames: 2/153 = CmsgProductConfirm (CmsgPetSummon refuted, ONE 4-byte builder);
      5/73 = SmsgQuestComplete (stale IDB autoname SmsgGuildWarInfoUpdate dropped);
      1/7 = CmsgSelectCharacterSlot ([u8 slot][u8 mode]; refutes "delete-mode");
      1/14 = CmsgMoveCharacterSlot (1-byte slot placement, not "delete/move");
      4/81 = SmsgActionErrorResult (generic action-error; the guild-diplomacy verdict rides 4/61,
      not 4/81); 4/74 = SmsgStallListRefill (variable record list);
      3/23 = SmsgCharStatusBytesByName (28B, keys by NAME);
  (2) reclassify: 4/56 (1552B) and 4/71 (1092B) are STRUCTURED PANELS, not thin slots;
  (3) size fixes (binary wins): 1/19 = 22 bytes; 1/16, 1/17 = header-only; 3/6 = 12; 3/13 = 19;
      3/14 = 16; 3/100 = 4; 0/0 = 62; the major-2 pinned sizes (2/113=8, 2/40=72, 2/50=16,
      2/55=32, 2/141=76, 2/72=40, 2/146=16, 2/52 short=32/full=var); the major-4 size fixes
      (4/19=56, 4/22=36, 4/50=32, 4/74=var, 4/115=24, 4/139=24, 4/24=44, 4/40=164, 4/4=892,
      4/48=236, 4/56=1552, 4/71=1092, 4/102=476, 4/65=1812); the major-5 size fixes
      (5/93=16, 5/57=24, 5/68=452, 5/94=16);
  (4) earlier-pass facts retained: major-3 ladder de-swapped (3/4 = SmsgSceneEntityUpdate,
      3/7 = SmsgCharManageResult, 3/14 = SmsgCharSpawnResponse, 3/100 = SmsgCharActionResult,
      no case 32); 2/28 CmsgQuestAction FIXED 12; 2/112 CmsgKeepaliveToggle distinct from
      2/10000; frame `size` is a true u32.
  Zero-gap: the in-scope rows are set-equal to the install-table surface (0/0 + major-1
  {C2S 0,2,4,6,7,9,13,14; S2C 16,17,19,20} + 95 distinct major-2 C2S opcodes + major-3
  {1,4,5,6,7,8,13,14,23,100,50000} + major-4 100 installed slots + {500,50000} + major-5
  65 installed slots). Residual: the wire VALUE semantics (the §3 capture/debugger register)
  + a curated, non-exhaustive major-2 listing (the additive coverage-gap note below the table).
byte-audit-2026-06-24 (lanes 14, IDB sha256 263bd994c927c20a38624cf0ca452eaef365057fa9db1543d8f668c14a6fd8ee,
  static-ida): 14-lane opcode audit settled the following corrections — all binary-wins, zero
  id/direction drift found across 80+ audited rows. Key changes applied this pass:
  (1) 2/6 renamed CmsgEmote (motion/emote send; drops the stale stance semantics; real stance toggle
      is 2/137); 2/137 renamed CmsgPostureToggle (f32 body fields are u8 casts, not world coords;
      reply = 5/126 SmsgActorStanceSet); 2/141 renamed CmsgCubeGambleSubmit (Cube-Gamble panel
      submit; not material-slot craft); 2/5 context region corrected to u8[20]@8 (not u8[24]@8, name
      at @0x08); 2/75 prose corrected to verbatim 80-byte panel sub-struct (20-dword block copy);
  (2) 4/23 Notes rewritten to selector@8/reason@9 (binary refutes prior phase-byte@10 layout);
      4/1 size var->9100 (fixed handler read 0x238C); 4/4 size 892->var (892 = tag-1/2/3 record
      size, not the packet size); 4/40 renamed SmsgMotionWeaponFxSwap; 4/71 renamed
      SmsgRelationPartyPanelSnapshot; 4/75 renamed SmsgProductPurchaseResultPanel + size var->184;
      4/78 renamed SmsgRentalItemExpirySweep + size var->12; 4/80 size var->80; 4/103 size
      var->204; 4/109 size var->12;
  (3) 5/10 size var->20; 5/31 size var->56; 5/67 Notes: XP i64 @0x10 (not @0x1C), stats at
      0x08/0x0C/0x18/0x1C/0x20; 5/68 Notes: count 20 (not 10), gate flag @0x09 (not @0x08);
      5/73 confirmed SmsgQuestComplete (no guild-war attribution); 5/86 Notes: field offsets +8
      (Mode@0x08, ValueA@0x0C, ValueB@0x10); 5/89 size var->188; 5/90 size var->32; 5/91 size
      var->124; 5/92 size var->40; 5/131 size var->8; 5/147 size var->8.
  Names-lane nuances noted (not catalog corrections, require names.yaml + YAML reconciliation):
  1/2 IDB autoname Cmsg_GameConnKeepalive_Send; 1/4 catalog name CmsgAuthReply vs spec
  CmsgLoginCredential; 2/21 CmsgListSelectAction; 2/40 CmsgSubmitRecord40; 2/41 CmsgHudSelect41;
  2/42 CmsgContextAction42; 2/19 single spec (CmsgNpcBuyOrAcquire); 4/5 YAML field offsets
  shifted ~4 vs handler (binary-wins note, YAML fix deferred to spec-author); 5/146 IDB autoname
  SmsgPacketResponseAckRequest vs catalog SmsgAckRequest.
login-handshake re-confirmation (2026-06-18, static-ida, anchor 263bd994, capture_verified: false):
  the inbound 0/0 key-exchange (62-byte fixed payload) and the reactive C2S 1/4 secure
  credential reply were re-walked from the dispatch glue and confirmed CONSISTENT with the
  existing rows. The frame header `major`/`minor` are confirmed to travel as TWO SEPARATE
  little-endian u16 words on the wire (`[u32 size][u16 major][u16 minor]`), NOT a single packed
  dword; the `(major<<16)|minor` form used in the Opcode column is a catalog convention only,
  never a literal the binary stores or compares.
cycle14: CYCLE 14 re-anchor (2026-06-27, static-ida, anchor f61f66a9ae0ec1e946105b2ecff76e8930cb1d1367df64e5688a5266f5ad9963):
  dispatch cluster re-anchor. Wire header layout, dispatch shape, table bases, and slot maps
  confirmed SAME on build f61f66a9. Three catalog corrections (binary wins):
  (1) 5/59 SmsgRankProgressSfxEvent — body size confirmed 0x4C (76 bytes; updated from "var");
  handler function lost its IDB name in the rebuild (Push slot minor 59, table index 1459;
  routing and behavior unchanged; catalog name retained; restore rename pending, ida-toolsmith).
  (2) 5/93 Push slot (SmsgActorMobIdChange) — live IDB MISLABELS this slot as SmsgVoteResult;
  catalog name SmsgActorMobIdChange confirmed correct by handler binary debug string (logs
  actor_sort, actor_id, old_mob_id, new_mob_id); corrective rename pending (ida-toolsmith).
  (3) 5/94 Push slot (SmsgVoteResult) — live IDB labels this SmsgVoteResult_0 (dedup suffix);
  catalog name SmsgVoteResult confirmed correct by handler debug string + MessageDB refs
  54098-54101; rename dropping _0 suffix pending (ida-toolsmith).
cycle15: CYCLE 15 re-anchor (2026-06-30, static-ida, anchor f61f66a9ae0ec1e946105b2ecff76e8930cb1d1367df64e5688a5266f5ad9963):
  CONSUMER-CONFIRMED this cycle (all on build f61f66a9):
  (1) 3/6 error-code→message-id map confirmed: {0xC8/0xC9→1106, 0xCE→1105, 0xCC/0xCD/0xCF..0xD2→1107, 0xD4→1113}. Shared pool also used by 3/13, 3/14, 3/100.
  (2) 3/7 Result polarity confirmed (1=success, 0=failure). Subtype correction: 0=face-slot-back, 1=face-slot-front (NOT "rename-applied"), 2=delete. The "rename-applied" label is REFUTED — rename rides 3/6.
  (3) 3/100 in-world code→message-id confirmed: 1→1201, 2→1202, 3→1203, 4→1204, 5→1205, 6→1206, 7→1207, 22→1208; select code 23→1604; generic→1107.
  (4) 4/15 failure subtype map confirmed (1..5→messages 10001..10005; 0/other=close-panels, no message); success 100=client-resolved free-slot insert, 101=plain insert, else=insert+world-entity despawn by key@+0x0C.
  (5) 4/61 body size confirmed FIXED 52 bytes. Gate byte @+8 (non-zero=apply-branch/zero=feedback-branch). Action×result matrix (consumer-confirmed). Cooldown at action 0/result 2 = delta in minutes (attribute slot 93), displayed as floor(minutes/1440) days.
  (6) 4/81 full error-code→message-id map confirmed: 0xFF→21078; 0x01-0x14/0x17-0x18 enumerated; 0x15=mm:ss cooldown (seconds@+10); 0x17=client-config-table percent (NOT a wire byte). status@+8 (1=apply/non-1=error); errorCode@+9; seconds@+10.
  (7) 4/99 sub-kind at +0x08 (1=bet-echo/else=result-announce). Bet-echo: bet-type@+0x0A (15/16/17/18=bet-slots, 100=chip-buy), wager@+0x0C. Result: codes 1..8→message-ids 58001..58030.
  (8) 4/100 phase@+0x08 {3=land, 4=intermediate, 5=spin-start/settle}; reel-digit@+0x0A (0xFF=reset); settled-money u64@+0x10; board-block 152B@+0x24. Match evaluator: win/loss from signed delta newMoney−oldMoney.
  (9) 2/23 mode-byte map confirmed: {0=Accept, 1=Decline, 2=Request/initiate}.
  (10) 2/35 mode-byte map confirmed: {0=Accept, 1=Decline, 2=Invite/initiate}.
  (11) 2/36 mode-byte map confirmed: {0=Leave(self), 1=Kick/expel}.
  (12) 2/37 mode-byte map confirmed: {0=DelegateLeader} (only value emitted by this client).
  (13) 2/141 body layout confirmed: 56B bet-amounts block + 16B bet-line block + 1B dealer-table-index + 3B alignment = 76B total. Auto-submitted on phase-3 land with pending bet.
  (14) 2/142 Op formula confirmed: Op = widget_action_id − 7. Layout: context_id u32@+0, op u8@+4, pad 3B@+5, value i64@+8.
  (15) 2/151 selector confirmed: {0=gold-shop→reply 3/8, 200=cash/Diamond-shop→reply 4/113}.
  (16) 2/153 reply opcode confirmed: 4/79 SmsgCraftingResult (NOT 3/8 or 4/113).
  (17) 5/5 inner_event_code visual meanings confirmed: 1001=world-effect+context-sound, 1011=disguise-apply, 1020=rename(17B CP949), 1021=motion-change(low16 src+0x208→tgt+0xA2), 1023=world-effect+sound 863000001, 1041=set-tgt-in-combat-flag.
  (18) 5/52 per-target record layout confirmed: actor composite key {sub-key u8@+0x00, key u32@+0x04}; i64 damage {lo-u32@+0x14, hi-u32@+0x18}; +0x08/+0x0C/+0x10 opaque; +0x1C..+0x23 opaque tail.
  (19) 5/53 field layout confirmed: sort u8@0, actor_id u32@4, relation_state u8@0x0A, relation_state_2 u8@0x0B, partner_actor_id u32@0x0C, HP-lo u32@0x10, HP-hi u32@0x14 (one i64), MP i32@0x18, stamina i32@0x1C. Status upgraded confirmed.
  (20) 5/79 body size confirmed FIXED 20 bytes. DeathOp@+0x08: {0=kill-setup, 1=spawn-death-effect (id=350000038+sub-selector@+0x0C), 2=set-substate-6-or-7, 3=revive/clear}.
  (21) 5/80 body size confirmed FIXED 16 bytes. Mode@+0x0A: {1=PvP-engage/spawn 371003701, 6=disengage/deactivate+spawn 371003702}; gate@+0x08 must be 1 for effect spawn.
  (22) 5/124 visual-flags bitmask byte at payload +0x08 written to actor +0x378. Bit 0x04 = paired-companion scale/motion. Other bits unenumerated.
  (23) 5/127 stealth_flag is boolean: nonzero=stealth ON (remote viewers release all visual nodes, actor hidden); 0=stealth OFF (rebuild skin, actor shown). Local player exempt from the visual toggle (flag stored but visual skipped).
-->

The clean-room source of truth for the Martial Heroes wire protocol. Consumed by the
`network-protocol-engineer` and validated by the `opcode-catalog` skill.

> **Routing/sizes/offsets confirmed; wire VALUE semantics capture-pending.** No live network
> capture was available during this analysis. Every opcode->routing link below is a hard static
> fact (read from the client's dispatch installer and the inbound dispatcher's switch arithmetic);
> the C2S send sizes and the S2C handler read sizes/field offsets are likewise control-flow-confirmed
> (read directly from the packet builders and handler read-sites). What remains **capture/debugger-pending**
> is the **field VALUE semantics** — what an individual wire byte *means* — wherever a builder blits
> an opaque caller blob or a handler forwards a body to a consumer. Treat every field *meaning* as a
> hypothesis until a capture confirms it; the offsets/sizes themselves are firm.

**Clean-room rules for this file:**
- **No IDA addresses** here. Address-level discovery lives in gitignored `_dirty/`; this catalog
  carries only promoted, neutral facts.
- Direction is from the client's point of view: `C2S` (client->server) or `S2C` (server->client).
- Each opcode with a known payload links to its field spec under `packets/`.

## Wire frame header

Every frame on the primary game connection begins with an 8-byte header, **little-endian**,
followed by the payload. The receive loop frames purely on these 8 bytes.

| Offset | Size | Field   | Meaning |
|--------|------|---------|---------|
| +0     | u32  | `size`  | Total frame size in bytes, including this 8-byte header. **The field is a true u32** — the long-standing u16-vs-u32 question is RESOLVED in favour of u32: the I/O loop frames on the full 32-bit value, so the bytes @+2 are part of `size`, not padding. (Header-only frames where `size == 8` bypass both the outbound cipher and compression.) |
| +4     | u16  | `major` | Opcode high part / message family. Selects the dispatch group. |
| +6     | u16  | `minor` | Opcode low part / message id. Selects the handler within the group. |
| +8     | ...  | payload | Message body, read sequentially from payload offset 0. |

Key facts an implementer must rely on:

- **The `(major:minor)` pair IS the opcode.** There is no separate opcode field. `major` and `minor` are **two separate little-endian u16 words** on the wire (`major` at +4, `minor` at +6) — they are NOT a single packed dword. The `major << 16 | minor` form used in the catalog's `Opcode` column is a project-side canonical key only, never a literal the binary stores or compares. (E.g. opcode 1/4 is the header bytes `01 00 04 00`, not a single packed dword formed by `major << 16 | minor`.)
- **No sequence number, no checksum.** The header carries only size + major + minor; frames are
  not signed.
- **The transform pipeline is ASYMMETRIC** (resolves the long-standing crypto open question #1):
  - **Outbound (C2S):** prepend a `GetTickCount` timestamp -> keyless byte cipher -> LZ4 compress -> send.
  - **Inbound (S2C):** LZ4 decompress ONLY (into a fixed 11680-byte buffer) -> route. **There is NO
    inverse byte cipher on the receive path.** The cipher routine has exactly one cross-reference —
    the outbound send gate — so it is structurally unreachable from the receive path (a positive
    single-caller proof). On the client side, server->client payloads are therefore compress-only;
    generalizing this across *all* inbound message types stays capture-pending, but the client never
    deciphers an inbound frame.
- **Some frames are compressed** (LZ4-family). A compressed frame is expanded into a fresh buffer
  *before* the `(major:minor)` dispatch, so handlers always see the decompressed payload.
- Minimum 8 bytes are required to parse a header. All packet-spec field offsets are
  **payload-relative** (relative to frame +8).

### Dispatch shape (how the inbound router and the C2S families are wired)

- **Majors 1 and 3 are inline switches** on the minor (enumerated minors; no default case). Major 1
  wires only minors 16/17/19/20 (S2C billing/letter); major 3 wires minors 1/4/5/6/7/8/13/14/23/100,
  plus the 50000 special (GM chat).
- **Majors 4 (Response) and 5 (Push) are TABLE-driven** — two 154-slot function-pointer tables. The
  Response slot is `handler-base + 1246 + minor` (dwords); the Push slot is `handler-base + 1400 + minor`.
  Every unset slot is pre-filled with a single inert no-op handler (not null), and `minor >= 154` is
  not dispatched. The two specials **4/500** and **4/50000** are routed *outside* the Response table.
  The installer wires **100 Response slots (99 distinct handlers -- minors 143/144 share one) plus the
  2 specials**, and **65 Push slots** (CYCLE 4 netcode-deep: confirmed against the live installers).
- **Major 0 is a hardwired `(0,0)` handshake branch** (not a switch): it parses the key blob and
  sends the C2S 1/4 reply.
- **Major 2 is C2S-only** — there is no inbound `case 2:` in the dispatcher. Every 2/* opcode is a
  client send.

### Opcode encoding in this catalog

The `Opcode` column encodes the `(major:minor)` tuple as a single hex literal
`major << 16 | minor` (e.g. 5/13 -> `0x5000d`, 4/13 -> `0x4000d`, 0/0 -> `0x0`). The
human-readable `major:minor` form is repeated at the start of every `Notes` cell. The major
family is the high 16 bits; the minor is the low 16 bits.

## Major families

| major | Family | Direction | Routing |
|-------|--------|-----------|---------|
| 0 | KeyExchange | S2C (handshake) | Hardwired `(0,0)` branch (NOT a switch): parses the key blob and triggers the C2S 1/4 auth reply. |
| 1 | Account / CharacterMgmt | C2S **and** S2C | Mixed family. **S2C** = an inline switch wiring only minors 16/17/19/20 (server-command notifications: billing / letter; no default case). **C2S** minors 0/6/7/9/13/14 are the character-management request builders (logout / create / select-slot / enter / rename / slot-move). **No major-1 char-delete opcode exists on this build** (deletes ride the major-3 result ladder). The secure 1/4 auth-reply (credential carrier) is also emitted under major 1. |
| 2 | GameAction | **C2S** | Client-emitted; **no inbound handler exists** (no `case 2:` in the dispatcher). Mostly user actions; also carries BOTH keepalive-family sends — the timer-driven 2/10000 (4-byte zero body, compress-only) and the on-demand 2/112 toggle. The binary has **96 distinct 2/* send builders mapping onto 95 distinct `(2:minor)` opcodes** (the 2/52 collision = two builders); CYCLE 4 catalogued the **complete** set — all 95 are individually listed below (see the closed coverage note under the table). |
| 3 | CharacterMgmt | S2C | Inline switch on minor (enumerated minors 1/4/5/6/7/8/13/14/23/100 + the 50000 special). |
| 4 | Response | S2C | Table-driven (acks / results): slot = `handler-base + 1246 + minor` (dwords), 154-slot bound, unset slots = inert no-op, `minor >= 154` not dispatched. Minors 500 and 50000 are special-cased outside the table. 100 slots installed (99 distinct handlers; 143/144 share). |
| 5 | Push | S2C | Table-driven (world events): slot = `handler-base + 1400 + minor` (dwords), 154-slot bound, unset slots = inert no-op. 65 slots installed. |

## Catalog

| Opcode | Name | Direction | Size (bytes) | Packet spec | Status | Notes |
|---|---|---|---|---|---|---|
| `0x0` | SmsgKeyExchange | S2C | 62 | packets/0-0_key_exchange.yaml | draft | (0:0) RSA key exchange; first secure-session frame. **62-byte fixed payload (debugger-verified)**: a 54-byte two-bignum key blob (server modulus + exponent) then two trailing 4-byte server scalars. Client immediately replies with the C2S 1/4 secure auth-reply. Layout per crypto.md section 6.2. |
| `0x10000` | CmsgLogout | C2S | 0 | packets/cmsg_logout.yaml | confirmed | (1:0) Logout / quit-from-in-game request. Header-only (zero payload); the only char-mgmt builder that does not set the in-flight latch. Single emitter (quit-from-in-game scene path). |
| `0x10002` | CmsgLobbyPing | C2S | 0 | packets/cmsg_lobby_ping.yaml | confirmed | (1:2) Lobby ping / heartbeat request. Header-only; sent from login/lobby. |
| `0x10004` | CmsgAuthReply | C2S | var | packets/login.yaml | draft | (1:4) **THE login credential send** -- the client secure auth-reply / login-credential carrier, and the REACTIVE reply to the inbound 0/0 key exchange. Emitted from the inbound major-0 branch right after the 0/0 key exchange, over the secure-send path. Its RSA pre-image is the `[0x2B][u32len account ]([u32len PIN ])` blob: the 1/4 frame carries TWO regions in one payload -- first the plaintext login pre-image (sub-opcode 0x2B + length-prefixed account + optional length-prefixed PIN), then the RSA ciphertext of the account password (PKCS#1 v1.5 type-2). The account PASSWORD rides INSIDE this 1/4 ciphertext. **Frame major/minor = 1/4 and the 0x2B credential prefix are debugger-verified.** DISTINCT from CharacterMgmt 1/6, which is **character-create ONLY** -- there is no shared login/create opcode (front-end deep-fidelity pass (campaign4/frontend-deep), dispatch-table-confirmed; the prior '1/6 login-or-create collision' is resolved). Layout per packets/login.yaml + crypto.md section 6. |
| `0x10006` | CmsgCreateCharacter | C2S | 52 | packets/cmsg_char_create.yaml | confirmed | (1:6) **Character-CREATE ONLY** -- collision RESOLVED (front-end deep-fidelity pass (campaign4/frontend-deep), dispatch-table-confirmed). Fixed 52-byte body whose **offset-0 is the CP949 character name, NEVER the 0x2B login blob**; the login credential rides 1/4, not 1/6 (see 0x10004). The single emitter is the create-confirm control of the character-select window's create sub-form (an in-place transient sub-state, not a separate scene). 52-byte embedded appearance/creation record, field map: Name @0 (18B CP949, min 2), Face @0x12 (1..7), Sex @0x14, hair/reserved @0x16, INTERNAL ClassId @0x18 (UI button->internal remap {0->4,1->1,2->3,3->2}, all four branches code-confirmed; matches frontend_scenes.md S4.1), 2-byte gap @0x1A, five u32 stats @0x1C, remaining-points u32 @0x30. Server replies S2C 3/6/3/23 create result. CAVEAT (capture-pending): the create-body field semantics at body +0x14/+0x16 (sex vs hair vs pad) and the trailing remaining-points/budget dword remain unverified. |
| `0x10007` | CmsgSelectCharacterSlot | C2S | 2 | packets/cmsg_char_select.yaml | confirmed | (1:7) Character-select-slot request on the char-select screen -- dual-purpose 2-byte body `[u8 slot][u8 mode]`: slot index @0 + a MODE byte @1. **Mode 1 = select-and-play; mode 0 = slot-lock** (CYCLE 4 netcode-deep correction, the binary wins -- this REFUTES the prior "delete-mode" reading). There is NO major-1 char-delete opcode on this build; deletes ride the major-3 result ladder. Prior name `CmsgManageCharacter` is superseded. Field VALUE internals capture-unverified. |
| `0x10009` | CmsgEnterGameRequest | C2S | 40 | packets/cmsg_char_enter.yaml | confirmed | (1:9) Enter-world request: slot index @0 + a 33-byte launcher-supplied session/identity token string (copied from the process command-line/argv0, not the typed account) + a 4-byte derived version token. The version token is NOT a constant: it is 10 x (field 5 of the on-disk data/cursor/game.ver) + 9. Enter sends ONLY 1/9 (the 1/7 manage was already sent at slot click). Single emitter (enter-selected-character helper). Server replies 3/5 EnterGameAck (scene -> loading), then 4/1 carries the world snapshot. The intra-buffer offsets are now statically pinned: slot @0x00, 33-byte session token @0x01, version dword @0x24. |
| `0x1000d` | CmsgRenameCharacter | C2S | 18 | packets/cmsg_char_rename.yaml | confirmed | (1:13) Rename-character request: slot index byte @0 + 17-byte CP949 new-name C-string @1 (<=16 usable). Triggered from the rename dialog's confirm button after charset + min-length-2 validation (NOT a slot-grid button). Server replies 3/6 RenameCharResult (and a 3/4 subtype-1 refresh). Field internals capture-unverified. |
| `0x1000e` | CmsgMoveCharacterSlot | C2S | 1 | packets/cmsg_char_move.yaml | confirmed | (1:14) Slot-MOVE / relocate request: a 1-byte target slot index (slot placement) -- NOT a delete (CYCLE 4 netcode-deep correction, the binary wins). 1/14 is the slot-move; delete instead rides the major-3 result ladder. Gated by slot<=4 and the in-flight latch; two emitters in the select-window dispatcher. Prior name `CmsgMoveCharacter` / the older "delete" label are superseded. Field VALUE internals capture-unverified. **CONTESTED LABEL (CYCLE 12 deferred):** send-site debug strings read "is sending location" (MOVE/relocate) but the only S2C reply it triggers — 3/7 SmsgCharManageResult sub-byte=2 — performs deletion (decrements char count, clears slot, shows delete-cooldown); no separate C2S delete opcode exists; canonical name (move vs delete) is capture-pending. |
| `0x10010` | SmsgSrvBillingDeactivated | S2C | 0 | packets/1-16_srv_billing_deactivated.yaml | confirmed | (1:16) Billing turned off. ROLE: resets billing status flag and formats a notice. Header-only. |
| `0x10011` | SmsgSrvBillingActivated | S2C | 0 | packets/1-17_srv_billing_activated.yaml | confirmed | (1:17) Billing turned on. ROLE: sets billing status flag and formats a notice. Header-only. |
| `0x10013` | SmsgSrvBillingExpiryNotice | S2C | 22 | packets/1-19_srv_billing_expiry_notice.yaml | confirmed | (1:19) Billing expiry notice. **SIZE CORRECTED to 22 bytes (1+1+20) (CYCLE 4 netcode-deep, the binary wins -- the prior 21/20 readings are stale).** ROLE: formats Day-Block / Hour-Block / Time-Block warnings. Payload: a 1-byte notice type + a 1-byte sub-field + a 20-byte block (16-char code + u32 time remaining). Field VALUE semantics capture-unverified. |
| `0x10014` | SmsgSrvLetterReceived | S2C | var | packets/1-20_srv_letter_received.yaml | confirmed | (1:20) In-game letter/mail received. ROLE: delivers a 76-byte mail header (ID, sender, type, attachments, date, subject) followed by a length-prefixed BodyText string. |
| `0x20000` | CmsgLeaveWorld | C2S | 0 | packets/2-0_leave_world.yaml | confirmed | (2:0) Leave-world / logout-to-lobby notice, header-only (0-byte body), emitted from the leave-world-to-logout scene path. Routing + header-only send `[confirmed]`; the role and any wire VALUE semantics `[capture/debugger-pending]`. |
| `0x20002` | CmsgPortalTravel | C2S | 4 | packets/2-2_portal_travel.yaml | confirmed | (2:2) Portal / teleport travel request, fired automatically on portal contact (raised from the 4/13 position-sync handler). Fixed 4-byte body (a target id u32); sets a netclient in-flight latch. Routing + 4-byte fixed size `[confirmed]`; the target-id meaning and any further VALUE semantics `[capture/debugger-pending]`. |
| `0x20003` | CmsgDialogResponse | C2S | 2 | packets/2-3_dialog_response.yaml | confirmed | (2:3) Relink / connection confirm-dialog response. Fixed 2-byte body (a u16 response code, observed range 0..3). Routing + 2-byte fixed size `[confirmed]`; the response-code enum `[capture/debugger-pending]`. |
| `0x20005` | CmsgActorInteraction | C2S | 28 | packets/2-5_actor_interaction.yaml | confirmed | (2:5) General NPC/actor world interaction request. Fixed 28-byte body: InteractionType u8 @0, SlotIndex u8 @1, ChannelByte u8 @2, reserved u8 @3, TargetActorId u32 @4, **Context u8[20] @8** (zeroed for general; type-specific 17-byte CP949 name begins at body **@0x08** for name-keyed interaction types, not @0x0b; map-id for cross-area). **CORRECTED (2026-06-24 byte-audit, binary wins — prior `u8[24]@8` overstated by 4 bytes; the body region @0x08..0x1B is 20 bytes; frame total 28 bytes is unchanged.)** A large caller-side switch branches on actor type (monster→2/139, quest NPC→context, storage, rename NPC with name, etc.); many branches route to UI info panels instead of sending. Send size (28 bytes) is control-flow-confirmed; field VALUE semantics capture-unverified. |
| `0x20006` | CmsgEmote | C2S | 8 | packets/2-6_stance_swap.yaml | confirmed | (2:6) **RENAMED CmsgEmote (2026-06-24 byte-audit, binary wins — the prior CmsgStanceSwap label and StanceId/Flag/TargetActorId semantics are dropped).** Motion/emote send. Fixed 8-byte body: MotionId u8 @0, Sub u8 @1, pad[2] @2, Arg u32 @4. Client-side throttled at 2000ms. Changes local motion and swaps weapon FX; also invoked by the duel-result handler (S2C 4/43 SmsgPlayerInteractionResult). The real combat-stance/posture toggle is 2/137 CmsgPostureToggle. *(Packet-spec filename `2-6_stance_swap.yaml` retained — the name is the stale pre-re-attribution label; the YAML body is owned by the names/packets lane to reconcile.)* Send size (8 bytes) is control-flow-confirmed; field VALUE semantics capture-unverified. |
| `0x20007` | CmsgChat | C2S | var | packets/2-7_whisper.yaml | draft | (2:7) Single C2S sender for ALL everyday chat. **Wire shape (re-confirmed on build 263bd994, Campaign 10): a UNIFORM 19-byte (0x13) prefix** = channel code @0 + a 1-byte sub-selector @1 + a 17-byte target-name area @2 (zeroed when the channel has no target), then `[u32 textLen][text]`. The earlier "broadcast has no name, whisper adds 16 bytes" form-split is corrected: every 2/7 carries the 17-byte name area; it is simply zero for non-whisper channels. **The text length prefix EXCLUDES the trailing NUL** (text capped at 119 / 0x77 chars) -- note this differs from 3/21, whose prefix INCLUDES the NUL. The first byte IS the channel code; whisper is just one channel, not a separate opcode. Server echoes via 5/7. The 19-byte prefix layout + the length-prefix-excludes-NUL rule are control-flow-confirmed; the integer channel-code table is capture-pending. **EMOTE NOTE (HUD-II Wave 2): the emote picker's text-macro page sends its phrase through THIS opcode** -- a page-0 emote macro is copied into the chat input line and submitted as ordinary chat text, so there is **no dedicated emote opcode**. The graphical emoticon page sends **nothing** (it only drives a client-local over-head balloon); whether that balloon is server-broadcast is capture/debugger-pending. Do NOT invent a phantom emote opcode. |
| `0x20008` | CmsgHotbarSync | C2S | 241 | packets/2-8_hotbar_sync.yaml | confirmed | (2:8) Hotbar / quickslot snapshot upload — a 240-slot scan staged into a fixed 241-byte body (a leading count byte then 240 slot-index bytes). Routing + 241-byte fixed size `[confirmed]`; the per-slot byte meanings `[capture/debugger-pending]`. |
| `0x2000c` | CmsgSelectTargetActor | C2S | 3 | packets/2-12_select_target_actor.yaml | confirmed | (2:12) Target / escort-actor select request. Fixed 3-byte body (a mode byte, a sub byte, a slot-index byte). Routing + 3-byte fixed size `[confirmed]`; the mode/sub/slot meanings `[capture/debugger-pending]`. |
| `0x2000d` | CmsgMoveRequest | C2S | 16 | packets/2-13_move_request.yaml | draft | (2:13) Client click-to-move / position sync: heading + target XZ + mode/run flags (16-byte fixed payload). Server echoes via 5/13. Layout capture-unverified. |
| `0x2000e` | CmsgDropItem | C2S | 8 | packets/2-14_drop_item.yaml | confirmed | (2:14) Drop an inventory item or coin stack to the ground: `mode u8 @0` (0=bag, 1=equip, 2=staged, 0xFF=coin), `slot u8 @1`, `count u32 @4`. The coin item id is 217000501. Major-2 is C2S-only (no inbound dispatch); the server answers with the 4/14 drop ack. Field layout capture-unverified. |
| `0x2000f` | CmsgPickupItem | C2S | 12 | packets/2-15_pickup_item.yaml | confirmed | (2:15) Pick up a targeted ground item: `key u32 @0` (ground-item entity key), three destination/share bytes @4/@5/@6 (coin path = 0xF0,0,0), `amount u32 @8`. The client pre-validates lock/busy/trade state, a free bag slot, the pickable flag, the level requirement (error 10005 if too low), and the coin gold cap. The server answers with the 4/15 pickup ack. Field layout capture-unverified. |
| `0x20010` | CmsgActorInteract | C2S | 12 | packets/2-16_actor_interact_mode100.yaml | confirmed | (2:16) Actor / NPC interaction request, mode-100 family. Fixed 12-byte body (a leading mode byte, four bytes, a 1-byte pad, then a u32 actor/arg word). Routing + 12-byte fixed size `[confirmed]`; the mode enum and arg meaning `[capture/debugger-pending]`. `[name collision — flagged for names lane]` (CmsgActorInteract is also staged for 2/139). |
| `0x20011` | CmsgServiceAction | C2S | 8 | packets/2-17_service_action.yaml | confirmed | (2:17) HUD-panel actor service action, relink-stateful. Fixed 8-byte body (a leading byte, a mode byte, a 2-byte pad, then a u32 arg). Routing + 8-byte fixed size `[confirmed]`; the mode/arg meanings `[capture/debugger-pending]`. |
| `0x20013` | CmsgNpcBuyOrAcquire | C2S | 12 | packets/2-19_npc_buy_or_acquire.yaml | confirmed | (2:19) NPC buy-or-acquire request via the inventory-driven shop transaction path; fixed 12-byte body {u32 id @0, u8 slot @4, u8 sub-sel @5, u8 flag @6, u8 flag @7, u32 qty @8}. **HUD-II Wave 3 re-attribution (build 263bd994):** the prior `CmsgNpcInteract` (generic NPC open / map-travel) reading for 2/19 is SUPERSEDED -- the 12-byte send is the buy/acquire transaction and it pairs with the already-catalogued 4/19 SmsgNpcBuyOrAcquireAck. The 12-byte size + layout are control-flow-confirmed; field VALUE meanings capture-pending. DISTINCT from the storefront buy 2/115. |
| `0x20014` | CmsgNpcSell | C2S | 12 | packets/2-20_npc_sell.yaml | confirmed | (2:20) Sell an inventory item to an NPC shop. **HUD-II Wave 3 (build 263bd994):** fixed 12-byte body {u32 id @0, u8 slot/mode @4, u8 sub-sel @5, 2-byte reserved @6, u32 qty @8} -- the stager leaves +0x06/+0x07 unwritten (reserved). Server answers via 4/20 SmsgNpcSellItemAck. The 12-byte size + layout are control-flow-confirmed; field VALUE meanings capture-pending. |
| `0x20015` | CmsgListSelectAction | C2S | 2 | packets/2-21_list_select.yaml | confirmed | (2:21) Short HUD-list / actor select action. Fixed 2-byte body (a mode byte, an index byte). Routing + 2-byte fixed size `[confirmed]`; the mode/index meanings `[capture/debugger-pending]`. |
| `0x20016` | CmsgServiceListAction | C2S | 8 | packets/2-22_service_list_action.yaml | confirmed | (2:22) HUD-list / actor service action, actor-validated. Fixed 8-byte body (a u32 mode word, an actor-index byte, then a 3-byte tail). Routing + 8-byte fixed size `[confirmed]`; the mode/actor-index meanings `[capture/debugger-pending]`. |
| `0x20017` | CmsgTradeRequest | C2S | 8 | packets/2-23_trade_request.yaml | confirmed | (2:23) Player-to-player trade request/respond. **SIZE CORRECTED from var to a FIXED 8-byte blit (CYCLE 2 netcode census, build 263bd994).** Layout: Mode u8@0, pad 3B@1, Id u32@4 (target actor composite id / actor+92). **CYCLE 15 mode-byte map (CONSUMER-CONFIRMED):** {0=Accept (confirm-popup commit), 1=Decline (popup decline button), 2=Request/initiate (opens 8 s confirm popup, context-code 706)}. Server drives the trade window via 4/23. Size + mode map control-flow-confirmed; pad bytes not zeroed by builder (re-impl should zero). |
| `0x20018` | CmsgTradeSlotAdd | C2S | 20 | packets/2-24_trade_slot_add.yaml | draft | (2:24) Add money/item to the local trade offer: leading category byte (0=money, 1=item, 4=add, 255=set-money) + an i64 money value carried as a base-1000 composite. Field layout capture-unverified. |
| `0x20019` | CmsgTradeConfirm | C2S | var | packets/2-25_trade_confirm.yaml | draft | (2:25) Confirm/cancel the trade: a 2-byte header followed by N x 4-byte own-side slot manifest entries (N<=40). Not a flat 2-byte packet. Field layout capture-unverified. |
| `0x2001c` | CmsgQuestAction | C2S | 12 | packets/2-28_quest_action.yaml | draft | (2:28) Quest accept/proceed/give-up action chosen by a sub-action selector (give-up == 4). **FIXED 12-byte body** `{u8 subAction @0, u32 @4, u32 @8}` (the send site appends exactly 12 bytes — the old `var` reading is corrected; control-flow-confirmed). Three callers: the NPC quest-talk confirm, the quest-talk on-event, and the quest give-up panel. Send-only: major-2 is C2S-only with NO inbound dispatch slot; the server answers via the 5/68 SmsgQuestList push (the quest-log snapshot) -- **and 5/73 is SmsgQuestComplete (CYCLE 4 netcode-deep rename, dropping the stale IDB autoname `SmsgGuildWarInfoUpdate`); the HUD-II Wave 4 "5/73 is GuildWarInfoUpdate, not a quest channel" note is itself superseded by the CYCLE 4 completeness register**. Field VALUE meanings capture-unverified. |
| `0x2001d` | CmsgStatAllocate | C2S | 20 | packets/2-29_stat_allocate.yaml | draft | (2:29) Stat-point allocation: five contiguous **absolute** u32 target stats in wire order STR, INT, AGI, DEX, CON (sent by the allocation window's Apply action). Server replies with 4/29 (stat-update ack). Field layout capture-unverified. |
| `0x2001e` | CmsgGuildOp | C2S | 8 | packets/2-30_guild_op.yaml | draft | (2:30) Guild operation request: u32 op + u32 id (8-byte body). Field layout capture-unverified. |
| `0x20048` | CmsgGmCommand | C2S | 32 | packets/2-72_gm_command.yaml | confirmed | (2:72) GM tool panel administrative command. **Fixed 32-byte body (CYCLE 4 netcode-deep re-pin: body 32, total wire frame 40 = 8-byte header + 32-byte body).** Field map: Mode u8 @0 (0=server-side, 1=client-side), TargetName char[16] @1 (CP949, NUL-padded), Padding u8[3] @0x11, Param1 u32 @0x14 (atol parsed), Param2 f32 @0x18 (atof parsed), Param3 f32 @0x1C (atof parsed). Two send paths: a manual single-command from the GM panel and a batch loop from command.txt. Administrative-only; normal players cannot access. Send size (32 bytes) is control-flow-confirmed; field VALUE semantics capture-unverified. |
| `0x2004c` | CmsgFateInteraction | C2S | 20 | packets/2-76_fate_interaction.yaml | confirmed | (2:76) Fate/destiny NPC interaction. Fixed 20-byte body: FateState u8 @0, SlotIndex u8 @1, ActorName char[17] @2 (CP949 target name), ItemListIndex u8 @0x13. Debug log reads 'FATE STATE[%d] ACTOR[%s] ITEM LIST[%d]'. Callers include ReAskPanel and the interaction dispatcher cases 34-47. Send size (20 bytes) is control-flow-confirmed; field VALUE semantics capture-unverified. |
| `0x20052` | CmsgGiftPresent | C2S | 28 | packets/2-82_gift_present.yaml | confirmed | (2:82) Send a gift/present to another player. Fixed 28-byte body: GiftIdOrProxy u32 @0, ParamFromUI u32 @4, FriendName char[17] @8 (CP949), Padding u8[3] @0x19. The caller reads the recipient name from the friend/gift panel and populates the two u32 ids from the UI state. Send size (28 bytes) is control-flow-confirmed; field VALUE semantics capture-unverified. |
| `0x20023` | CmsgPartyInvite | C2S | 8 | packets/2-35_party_invite.yaml | confirmed | (2:35) Party invite/accept: u8 mode@0 + pad 3B@1 + u32 Id@4 (target actor composite id / actor+92). **CYCLE 15 mode-byte map (CONSUMER-CONFIRMED):** {0=Accept (confirm-popup commit, context-code 800), 1=Decline (invite popup decline button), 2=Invite/initiate (opens 8 s confirm popup then sends)}. Layout (8-byte) static-confirmed. Server replies via 4/35/4/76. |
| `0x20024` | CmsgPartyLeaveKick | C2S | 8 | packets/2-36_party_leave_kick.yaml | confirmed | (2:36) Party leave-or-kick: u8 mode@0 + pad 3B@1 + u32 Id@4 (actor composite id / actor+92). **CYCLE 15 mode-byte map (CONSUMER-CONFIRMED):** {0=Leave(self) — Id is local player's own composite id; auto-disbands when <=1 member remains, 1=Kick/expel — Id is selected member's composite id; requires local player is party leader and target shares the same party id}. Layout (8-byte) static-confirmed. Pairs with 4/36. |
| `0x20025` | CmsgPartyLeaderOp | C2S | 8 | packets/2-37_party_leader_op.yaml | confirmed | (2:37) Party leader operation: u8 mode@0 + pad 3B@1 + u32 Id@4 (target member's composite id). **CYCLE 15 mode-byte map (CONSUMER-CONFIRMED):** {0=DelegateLeader — the only value emitted by this client; transfers party leadership to the targeted member}. No other mode value is produced by any caller. Layout (8-byte) static-confirmed. Pairs with 4/37. Any additional server-defined modes are capture-pending. |
| `0x20027` | CmsgHudContextAction39 | C2S | 12 | packets/2-39_hud_context_action.yaml | confirmed | (2:39) HUD context action, cost/limit-gated with a 2-stage target resolve. Fixed 12-byte body (a 6-byte prefix, three resolved bytes @6..8, then a pad). Routing + 12-byte fixed size `[confirmed]`; all field VALUE meanings `[capture/debugger-pending]`. |
| `0x20028` | CmsgSubmitRecord40 | C2S | 72 | packets/2-40_submit_record.yaml | confirmed | (2:40) Submit a full record from a HUD list (an opaque 72-byte record; a validator reads a selector byte at @0x44). Routing + 72-byte fixed size `[confirmed]`; the record's internal field layout `[capture/debugger-pending]`. |
| `0x20029` | CmsgHudSelect41 | C2S | 12 | packets/2-41_hud_select.yaml | confirmed | (2:41) HUD list confirm carrying three u32 keys. Fixed 12-byte body (3 × u32). Routing + 12-byte fixed size `[confirmed]`; the three key meanings `[capture/debugger-pending]`. |
| `0x2002a` | CmsgContextAction42 | C2S | 44 | packets/2-42_context_action.yaml | confirmed | (2:42) Context-action record (eleven u32 words; a mode byte at @0; an optional tooltip). Fixed 44-byte body. Routing + 44-byte fixed size `[confirmed]`; the per-word meanings `[capture/debugger-pending]`. |
| `0x2002d` | CmsgHudFlagsAction45 | C2S | 8 | packets/2-45_hud_flags_target.yaml | confirmed | (2:45) HUD flags + target action, gated by an inventory prerequisite. Fixed 8-byte body (four bytes then a u32). Routing + 8-byte fixed size `[confirmed]`; the flag/target meanings `[capture/debugger-pending]`. |
| `0x2002c` | CmsgItemQuickMove | C2S | 12 | packets/2-44_item_quick_move.yaml | confirmed | (2:44) **NEW (HUD-II Wave 4).** Shared item-model QUICK-MOVE / shift-transfer -- the shortcut transfer of an item to-or-from a container (no held-item drag). Fixed 12-byte body. **BYTE-SHAPE CORRECTED (CYCLE 2 netcode census, build 263bd994): the dedicated struct-packer writes FIVE bytes @0..4 (a leading mode/selector byte then four container/slot bytes), then a 3-BYTE gap @5..7, then a u32 context word @8 — head is 5 not 6 and the gap is 3 not 2.** The @8 context word is the held item-model id (0 when the keep window is open). This differs from 2/46's canonical 6+2-gap+4 model. **Storage-grid usage:** the 60-cell KeepPanel storage grid (slot 191) uses 2/44 to quick-transfer items into / out of storage (distinct from the storage open/money lane 2/142). The 12-byte size + the 6+gap+4 shape are control-flow-confirmed; the per-byte assignment (mode vs source/destination container & slot) and the Context meaning are capture/debugger-pending. |
| `0x2002e` | CmsgItemMove | C2S | 12 | packets/2-46_item_move.yaml | confirmed | (2:46) **NEW (HUD-II Wave 4).** Shared item-model MOVE / swap -- relocate an item between containers and slots (the drag-drop move). Fixed 12-byte body: a leading op/selector byte @0, five container/slot bytes @1..5, a 2-byte gap @6, and a context word @8 (held item-model id, 0 when the keep window is open). **Storage-grid usage:** the 60-cell (10x6) KeepPanel storage grid (slot 191) uses 2/46 to deposit / move items INTO and OUT OF the grid -- the unified destination slot is the storage block base (+56, +60 per page) plus the cell index. This is the item MOVEMENT lane for storage, distinct from the storage open/money lane 2/142. The 12-byte size + the 6+gap+4 shape are control-flow-confirmed; the per-byte assignment and the Context meaning are capture/debugger-pending. |
| `0x2002f` | CmsgHudCodeValue47 | C2S | 8 | packets/2-47_hud_code_value.yaml | confirmed | (2:47) Simple HUD (code, value) action, panel-modal-gated. Fixed 8-byte body (a code byte, a pad, then a u32 value). Routing + 8-byte fixed size `[confirmed]`; the code/value meanings `[capture/debugger-pending]`. |
| `0x20030` | CmsgHudSubmitId48 | C2S | 4 | packets/2-48_submit_id.yaml | confirmed | (2:48) Confirm / submit a single u32 id from a HUD list. Fixed 4-byte body (a u32 id). Routing + 4-byte fixed size `[confirmed]`; the id meaning `[capture/debugger-pending]`. |
| `0x20031` | CmsgFriendAddRemove | C2S | 19 | packets/2-49_friend_add_remove.yaml | draft | (2:49) Friend add OR remove, by name -- ONE opcode, a leading tag byte selects add (0) vs cut/remove (1). 19-byte (0x13) body: tag @0 + index @1 + 16-byte CP949 name @2 + 1-byte pad. Raised from the friend window add-name and cut-name text fields via the in-game command interpreter ("friend <name>" / "cut <name>"); there is NO separate search field. Body size + tag/name layout static-confirmed; field VALUE semantics capture-unverified. Inbound populate candidate = 5/26 (UNVERIFIED). |
| `0x20032` | CmsgHudRecord50 | C2S | 16 | packets/2-50_hud_record_16.yaml | confirmed | (2:50) Submit a 16-byte HUD record (closes the panel group first). Opaque 16-byte body. Routing + 16-byte fixed size `[confirmed]`; the record's internal layout `[capture/debugger-pending]`. |
| `0x20034` | CmsgUseSkill | C2S | 32 fixed / var | packets/2-52_use_skill.yaml | draft | (2:52) Client skill-activate request: a **24-byte (0x18) header + two OPTIONAL u16-counted u32 target-id arrays**. **TWO builders share this one opcode (CYCLE 4 netcode-deep): a SHORT form (fixed 32 bytes, built reactively in inbound handlers, no latch) and a FULL form (variable, player-driven, sets the combat latch). Empty target arrays make the full form also 32 bytes, so length alone does NOT distinguish the two -- the discriminator is the struct content.** Re-confirmed on build 263bd994 (Campaign 10): two builders share this opcode (a short header-only/basic-melee form and a full form); the full builder appends the 24-byte header, then -- if CountA (a u16 at header @0x14) is nonzero -- `4*CountA` bytes, then -- if CountB (a u16 at header @0x16) is nonzero -- `4*CountB` bytes (4-byte stride each). SkillSlot is the header byte @0; a slot byte of `0xFF` is a **basic melee attack** -- there is **no separate plain-attack opcode**; ordinary melee is 2/52 with slot `0xFF` (header-only, both counts 0). Server replies with 5/52. The 24-byte header, the two count words @0x14/@0x16, the 4-byte array stride, and the 0xFF-melee marker are control-flow-confirmed; the aim sub-fields @0x04..0x13 are caller-staged (not visible at the builder) and the @0x01 selector byte is a derived value -- both capture-pending. |
| `0x20036` | CmsgFriendListRefresh | C2S | 1 | packets/2-54_friend_list_refresh.yaml | draft | (2:54) Friend-list / online-status refresh request: 1-byte body, raised by the friend window refresh control. Client-side rate-limited (a 3-minute / 180000 ms throttle). Body size static-confirmed; the single request byte value + the server reply opcode are capture-unverified. Inbound populate candidate = 5/26 (UNVERIFIED). |
| `0x20037` | CmsgHudRecord55 | C2S | 32 | packets/2-55_hud_record_32.yaml | confirmed | (2:55) Submit a 32-byte HUD record (same caller cluster as 2/50 / 2/58). Opaque 32-byte body. Routing + 32-byte fixed size `[confirmed]`; the record's internal layout `[capture/debugger-pending]`. |
| `0x20038` | CmsgStallEnter | C2S | 4 | packets/2-56_stall_enter.yaml | draft | (2:56) **NEW (HUD-II Wave 4).** Enter / open a selected stall from the personal-stall (player-vendor) market list. Fixed 4-byte body = the selected stall row id (u32), copied from the chosen row record. Issued by the StallListPanel's ENTER action and a row double-click; pairs with the list request 2/74 / refill 4/74. The 4-byte size is control-flow-confirmed; whether the id is the stall primary key or the owner actor id is capture/debugger-pending. |
| `0x20039` | CmsgStackDistribute57 | C2S | var | packets/2-57_stack_distribute.yaml | confirmed | (2:57) Stack split / distribute submit, with a counted contributing-slot tail: a 20-byte fixed header (op, idx, slot, u16 qty, two u32) then a u8 tail-count and `tail-count` bytes. VARIABLE size. Routing + header layout + counted tail `[confirmed]`; all field VALUE meanings `[capture/debugger-pending]`. |
| `0x2003a` | CmsgHudPricedCommit58 | C2S | 24 | packets/2-58_priced_commit.yaml | confirmed | (2:58) Priced HUD commit on a slot (amount × unit-price vs gold; slot index @4, amount @8). Fixed 24-byte body. Routing + 24-byte fixed size `[confirmed]`; the field VALUE meanings `[capture/debugger-pending]`. |
| `0x2003c` | CmsgCcoupleRelationRequest | C2S | 8 | packets/2-60_letter_request.yaml | draft | (2:60) **RE-ATTRIBUTED to the COUPLE / MARRIAGE relation request (CYCLE 7, the binary wins -- this is NOT mail/letter).** An 8-byte fixed body built from the couple UI: a `{mode/action byte 0..4, partner actor id}` relation command (open / propose / accept / break a couple-partner link). The dev `Cmsg_LetterRequest_Send` autoname (and the prior "LetterPanel request" reading) is misleading -- the real mail feature is the delivery-inbox + carrier-pigeon (C2S 2/70 / 2/71, S2C 4/70), NOT 2/60. Its reply is **S2C 5/53 SmsgActorVitalsAndPairState**, which drives the couple/partner window (the IDB-named `PetPanel` is the couple window, not a pet UI -- this build has no tamed-creature pet feature). Send size (8 bytes) is control-flow-confirmed; the exact mode-byte vs partner-id split and all VALUE semantics are capture/debugger-pending. *(Packet-spec filename `2-60_letter_request.yaml` retained -- the "letter" in the name is the stale pre-re-attribution autoname; the linked yaml still carries the old CmsgLetterRequest body and is owned by the packets/names lane to reconcile.)* |
| `0x2003d` | CmsgPartyRequest | C2S | 36 | packets/2-61_party_request_union.yaml | confirmed | (2:61) Party-operations request, discriminated by a leading subcode. Fixed 36-byte union body (a subcode byte, an index byte, a 17-byte CP949 name, a u32 amount). Routing + 36-byte fixed size `[confirmed]`; the subcode union semantics `[capture/debugger-pending]`. |
| `0x2003e` | CmsgRelationNamedRequest | C2S | 19 | packets/2-62_relation_named_request.yaml | confirmed | (2:62) Name-keyed relation / party request with a type + flag. Fixed 19-byte body (a 16-byte CP949 name, a u16 type, a flag byte). Routing + 19-byte fixed size `[confirmed]`; the type/flag meanings `[capture/debugger-pending]`. |
| `0x2003f` | CmsgRelationNamedAction | C2S | 17 | packets/2-63_relation_named_action.yaml | confirmed | (2:63) Name-targeted party / guild action, class- and gold-gated. Fixed 17-byte body (a 16-byte CP949 name, a mode byte). Routing + 17-byte fixed size `[confirmed]`; the mode meaning `[capture/debugger-pending]`. |
| `0x20040` | CmsgClassChangeRequest | C2S | 8 | packets/2-64_class_change_request.yaml | confirmed | (2:64) Class-change / promotion request, validated against the class ladder. Fixed 8-byte body (a u32 class-selector, a u32 param). Routing + 8-byte fixed size `[confirmed]`; the selector/param meanings `[capture/debugger-pending]`. |
| `0x20041` | CmsgGuildContextAck | C2S | 1 | packets/2-65_guild_context_ack.yaml | confirmed | (2:65) Event-driven 1-byte context ack / refresh — REACTIVE, emitted from server-handler paths (the game-state tick and the guild-member-remove result), not a proactive player command. Fixed 1-byte body. Routing + 1-byte fixed size + reactive nature `[confirmed]`; the ack byte value `[capture/debugger-pending]`. |
| `0x20042` | CmsgRelationToggle | C2S | 1 | packets/2-66_relation_toggle.yaml | confirmed | (2:66) 1-byte relation toggle / answer (raised from the relation re-ask panel). Fixed 1-byte body. Routing + 1-byte fixed size `[confirmed]`; the toggle value `[capture/debugger-pending]`. |
| `0x20046` | CmsgCarrierPigeonSend | C2S | 132 | packets/2-70_carrier_pigeon_send.yaml | draft | (2:70) **NEW (HUD-II Wave 2).** Carrier-pigeon mail SEND: a 132-byte (0x84) FIXED body staged by the mail compose panel -- a CP949 recipient name (max 16 chars), the subject/message text region, an attached-item record array (up to 5 staged item records), and a money/postage field (postage gate = 200000 gold). Emitted from the send-confirm dialog's accept path. Total body size (132 bytes) is control-flow-confirmed; the intra-buffer field offsets (recipient / subject / body / item array / money split) are capture/debugger-pending. Mail-arrival notification rides S2C 1/20 SmsgSrvLetterReceived. |
| `0x20047` | CmsgDeliveryClaim | C2S | 4 | packets/2-71_delivery_claim.yaml | draft | (2:71) **NEW (HUD-II Wave 2).** Delivery / consignment claim-retrieve: a 4-byte fixed body (a single selector / record-id) that retrieves a delivered or consigned item (or sale proceeds) from the delivery box. Emitted from the delivery panel's claim-confirm path. Send size (4 bytes) is control-flow-confirmed; whether the 4 bytes are a record id, a slot index, or a side selector is capture/debugger-pending. |
| `0x2004a` | CmsgStallListRequest | C2S | 32 | packets/2-74_stall_list_request.yaml | draft | (2:74) **NEW (HUD-II Wave 4).** Request / search of the personal-stall (player-vendor) market list. Fixed 32-byte (0x20) body: a leading flag/mode byte @0 (=1 observed), a 30-char CP949 name-filter @1 (NUL-padded; empty = list all), and one trailing/version byte @31. Issued by the StallListPanel search-submit and refresh actions behind a ~10s client rate gate. Server replies via 4/74 SmsgStallListRefill. The 32-byte size + the flag/name/trailer layout are control-flow-confirmed; the Flag mode enum and the trailer value are capture/debugger-pending. |
| `0x2004b` | CmsgRosterAssemble | C2S | 80 | packets/2-75_roster_assemble.yaml | confirmed | (2:75) Roster / social-panel assemble request. **CORRECTED (W5.1 binary-wins correction): the earlier "8-byte header + ten u32 actor ids + 32-byte tail" model is withdrawn.** The builder appends a VERBATIM 80-byte resident panel sub-struct — a 20-dword block copy from the panel's live buffer. Only the +0x04 arm/status byte is field-confirmed at the send path (stamped to 1 on submit); the remainder is opaque interior panel state mixing ids and CP949 width-17 name cells, exact field split not recoverable from the send site alone. Three reach sites (goods/roster panel commit). **Re-confirmed (2026-06-24 byte-audit):** size (80), direction (C2S), and status (confirmed) are correct; interior field split and value semantics remain `[capture/debugger-pending]`. |
| `0x2004f` | CmsgActorTargetRequest | C2S | 16 | packets/2-79_actor_target_request.yaml | confirmed | (2:79) Actor-target / context-selection request, resolved through an index→actor table. Fixed 16-byte body (a leading selector byte, a table-index byte at @8, surrounding fields). Routing + 16-byte fixed size `[confirmed]`; the selector/table-index meanings `[capture/debugger-pending]`. |
| `0x20050` | CmsgGuildRequest | C2S | 48 | packets/2-80_guild_request_union.yaml | confirmed | (2:80) Guild / clan + map-territory request, a discriminated union (guild fame panel + map-pick). Fixed 48-byte union body (a subcode byte, a u32 id, a qword amount, a 17-byte CP949 name). Routing + 48-byte fixed size `[confirmed]`; the subcode union semantics `[capture/debugger-pending]`. |
| `0x20051` | CmsgGuildDiplomacyDeclare | C2S | 18 | packets/2-81_guild_diplomacy_declare.yaml | draft | (2:81) **NEW (HUD-II Wave 4).** Guild-diplomacy / brood-war declaration request, sent from the guild-relations list window (BroodWarListPanel). Fixed 18-byte (0x12) body: a leading action/state byte @0 + a 17-byte CP949 target guild name @1 (NUL-padded). The same 18-byte shape also serves the panel's open / relation-list request (carrying the local player name); whether the pure list-fetch uses a distinct minor is capture-pending. The actual send fires from a confirm-dialog OK callback after phase/level/gold/name prechecks. **Server replies via 4/61 SmsgGuildStateChangeResult (52-byte body) -- CORRECTED (CYCLE 4 netcode-deep, the binary wins): the diplomacy verdict rides slot 61, NOT 4/81 (4/81 is the generic SmsgActionErrorResult).** The 18-byte size + action/name layout are control-flow-confirmed; the Action value map (declare-war vs declare-ally vs list-request) is capture/debugger-pending. |
| `0x20053` | CmsgChatContextual | C2S | var | packets/2-83_chat_contextual.yaml | draft | (2:83) Client contextual chat: 24-byte context header + length-prefixed text. Header fields not yet decoded; layout capture-unverified. |
| `0x20054` | CmsgNpcMediatedNameRequest84 | C2S | 19 | packets/2-84_npc_mediated_name_request.yaml | confirmed | (2:84) NPC-mediated, name-keyed request (a 30s client throttle; routed through a nearby service NPC). Fixed 19-byte body (a kind byte, a nearby-NPC index byte (0xFF when none), then a 17-byte CP949 name). Routing + 19-byte fixed size `[confirmed]`; the kind/name meanings `[capture/debugger-pending]`. |
| `0x2005c` | CmsgRevengeSummon | C2S | 4 | packets/2-92_revenge_summon.yaml | confirmed | (2:92) Revenge-system "summon target" confirm. Fixed 4-byte body (a u32 target reference). Routing + 4-byte fixed size `[confirmed]`; the target-ref meaning `[capture/debugger-pending]`. |
| `0x2005d` | CmsgRevengeVote | C2S | 1 | packets/2-93_revenge_vote.yaml | confirmed | (2:93) Revenge-system vote yes/no response. Fixed 1-byte body (a vote flag). Routing + 1-byte fixed size `[confirmed]`; the vote-flag value `[capture/debugger-pending]`. |
| `0x2005f` | CmsgTriggerAction95 | C2S | 1 | packets/2-95_trigger_action.yaml | confirmed | (2:95) Fire-once confirm button with a client cooldown stamp. Fixed 1-byte body (a flag, observed value 1). Routing + 1-byte fixed size `[confirmed]`; the flag value `[capture/debugger-pending]`. |
| `0x20063` | CmsgActionToggle99 | C2S | 3 | packets/2-99_action_toggle.yaml | confirmed | (2:99) Action / stance toggle, raised by two command-group callers. Fixed 3-byte body (an action code byte, a category byte (1..4), a local flag byte). Routing + 3-byte fixed size `[confirmed]`; the action/category meanings `[capture/debugger-pending]`. |
| `0x20064` | CmsgNpcOpenReq | C2S | 1 | packets/2-100_npc_open_req.yaml | draft | (2:100) Open-request for the KIND-0x19 service NPC panel. **SIZE CORRECTED from var to a FIXED 1-byte blit (CYCLE 2 netcode census, build 263bd994): the builder appends a single fixed byte, not a variable tail.** The 1-byte fixed size is control-flow-confirmed; the single request-byte value is capture/debugger-pending. |
| `0x20067` | CmsgTextFormSubmit4103 | C2S | 196 | packets/2-103_text_form_submit.yaml | confirmed | (2:103) Four-line CP949 text-form submit. Fixed 196-byte body (four 49-byte CP949 text cells). Routing + 196-byte fixed size `[confirmed]`; the per-line semantics `[capture/debugger-pending]`. |
| `0x2006a` | CmsgCreatureItemTick106 | C2S | 1 | packets/2-106_creature_item_tick.yaml | confirmed | (2:106) Creature-item effect-tick acknowledgment. Fixed 1-byte body (a flag, observed value 1). Routing + 1-byte fixed size `[confirmed]`; the flag value `[capture/debugger-pending]`. |
| `0x2006b` | CmsgAmountCommit107 | C2S | 4 | packets/2-107_amount_commit.yaml | confirmed | (2:107) Numeric amount commit from a keypad accumulator. Fixed 4-byte body (a u32 amount). Routing + 4-byte fixed size `[confirmed]`; the amount meaning `[capture/debugger-pending]`. |
| `0x2006c` | CmsgNpcLocateRequest108 | C2S | 4 | packets/2-108_npc_locate_request.yaml | confirmed | (2:108) NPC search / locate request keyed by an on-disk NPC record id. Fixed 4-byte body (a u32 record id). Routing + 4-byte fixed size `[confirmed]`; the record-id meaning `[capture/debugger-pending]`. |
| `0x2006e` | CmsgQuestNpcStep | C2S | 4 | packets/2-110_quest_npc_step.yaml | draft | (2:110) Quest-NPC dialogue step advance (4-byte body). Field layout capture-unverified. |
| `0x20070` | CmsgKeepaliveToggle | C2S | 1 | packets/2-112_keepalive_toggle.yaml | confirmed | (2:112) **On-demand keepalive enable/toggle — DISTINCT from the timer-driven 2/10000.** The client has TWO separate keepalive-family C2S messages on major 2; this is the second one. 1-byte body (the send appends a single constant `1`). Gated by a master-enable flag (`g_KeepaliveEnabled`): the toggle is set on world-enter and cleared on leave; emitted from the leave-to-logout scene path, the game-state-tick handler, and `WinMain`. Whether the 1-byte 2/112 toggle, the cached 2/10000 frame, or both are actually put on the wire — and at what cadence — is capture/debugger-pending. Send size + the 1-byte body are control-flow-confirmed. |
| `0x20071` | CmsgRepairCommit | C2S | 8 | packets/2-113_repair_commit.yaml | confirmed | (2:113) Commit a repair at a repair NPC. **Fixed 8-byte body (CYCLE 4 netcode-deep re-pin, was unstated).** Server answers via the 4/113-family ack. Field VALUE layout capture-unverified. |
| `0x20072` | CmsgGatedSelfAction114 | C2S | 4 | packets/2-114_gated_self_action.yaml | confirmed | (2:114) Requirement-gated self-action, reads a local-player field. Fixed 4-byte body (a u32 player-state field). Routing + 4-byte fixed size `[confirmed]`; the field meaning `[capture/debugger-pending]`. |
| `0x20073` | CmsgShopBuy | C2S | 8 | packets/2-115_shop_buy.yaml | confirmed | (2:115) Buy/order commit from the NPC storefront list (item-shop vendor). **HUD-II Wave 3 size CORRECTION (build 263bd994): FIXED 8-byte body {u32 npc_id @0, u32 row*2 @4}** -- the prior `var` size is corrected. Server replies on 4/19 / 4/113 and the balance push 4/115. DISTINCT from the inventory-driven buy 2/19. The 8-byte size + two-word layout are control-flow-confirmed; field VALUE meanings capture-pending. |
| `0x20076` | CmsgTenderConfirm | C2S | 0 | packets/2-118_tender_confirm.yaml | draft | (2:118) **NEW (HUD-II Wave 2).** TenderInfoPanel buy/confirm request: a HEADER-ONLY frame (0-byte body, total frame size 8). Emitted from the tender/consignment info panel's confirm button and gold-gated (the panel checks the local player's gold against the listed cost before sending). Header-only send is control-flow-confirmed; the server populate that fills the tender listing is not yet pinned (capture/debugger-pending). |
| `0x20078` | CmsgEventItemListSubmit | C2S | var | packets/2-120_event_item_list_submit.yaml | confirmed | (2:120) Event-item list submit (raised from the event-item confirm panel). Count-prefixed VARIABLE body: a flag byte, a u32 record count N, then N × 12-byte records. Routing + the count-prefixed shape `[confirmed]`; the per-record meanings `[capture/debugger-pending]`. |
| `0x2007a` | CmsgGiftCharSend | C2S | 12 | packets/2-122_gift_char_send.yaml | confirmed | (2:122) Gift-character send with a second-password field. Fixed 12-byte body (a u32 id, a group byte, a 4-byte field). Routing + 12-byte fixed size `[confirmed]`; the id/group meanings `[capture/debugger-pending]`. |
| `0x2007b` | CmsgGiftCharReceiveConfirm | C2S | 12 | packets/2-123_gift_char_receive.yaml | confirmed | (2:123) Gift-character receive confirm. Fixed 12-byte body (an accept byte, a u32 offer id, a byte). Routing + 12-byte fixed size `[confirmed]`; the accept/offer meanings `[capture/debugger-pending]`. |
| `0x2007c` | CmsgItemPanelFlag | C2S | 1 | packets/2-124_item_panel_flag.yaml | confirmed | (2:124) Item-panel single-byte flag (raised from the item panel's on-event). Fixed 1-byte body. Routing + 1-byte fixed size `[confirmed]`; the flag value `[capture/debugger-pending]`. |
| `0x2007e` | CmsgConfirmDialogState | C2S | 1 | packets/2-126_confirm_dialog_state.yaml | confirmed | (2:126) Confirm-dialog state byte. Fixed 1-byte body (a state value, observed 1/2). Routing + 1-byte fixed size `[confirmed]`; the state-value enum `[capture/debugger-pending]`. |
| `0x20080` | CmsgPanelIdSubmit | C2S | 4 | packets/2-128_panel_id_submit.yaml | confirmed | (2:128) Panel u32-id submit. Fixed 4-byte body (a u32 id). Routing + 4-byte fixed size `[confirmed]`; the id meaning `[capture/debugger-pending]`. |
| `0x20086` | CmsgNpcSearchById | C2S | 24 | packets/2-134_npc_search_by_id.yaml | confirmed | (2:134) Dual-mode NPC search request (by ID or by name). Fixed 24-byte body: SearchId u32 @0 (NPC id; 0 when searching by name), NameFilter char[17] @4 (CP949, set when SearchId==0), Padding u8[3] @0x15. When SearchId is nonzero, searches by ID with name=null. When SearchId is 0, searches by the NameFilter string. Called from the NPC search panel flow. Send size (24 bytes) is control-flow-confirmed; field VALUE semantics capture-unverified. |
| `0x20087` | CmsgNpcSearchByName | C2S | 21 | packets/2-135_npc_search_by_name.yaml | confirmed | (2:135) Name-only NPC search request. Fixed 21-byte body: SearchName char[20] @0 (CP949), Pad u8 @0x14. Distinct from the dual-mode 2/134 (by ID or name). Single caller is the NPC search name-submit path. Send size (21 bytes) is control-flow-confirmed; field VALUE semantics capture-unverified. |
| `0x20089` | CmsgPostureToggle | C2S | 16 | packets/2-137_exit_notify.yaml | confirmed | (2:137) **RENAMED CmsgPostureToggle (2026-06-24 byte-audit, binary wins — prior CmsgExitNotify label and "exit/quit" role are dropped; the builder is the combat-posture/stance toggle).** Fixed 16-byte body: Stance u8 @0, Sub u8 @1, reserved u16 @2, ArgA f32 @4 **(cast from a u8 arg — NOT a world coordinate)**, ArgB f32 @8 **(cast from a u8 arg — NOT a world coordinate)**, Extra u8 @0x0C, Padding u8[3] @0x0D. S2C reply: 5/126 SmsgActorStanceSet. *(Packet-spec filename `2-137_exit_notify.yaml` retained — stale pre-re-attribution name; YAML body is owned by the packets/names lane to reconcile.)* Send size (16 bytes) is control-flow-confirmed; field VALUE semantics capture-unverified. |
| `0x2008b` | CmsgAutoInteract | C2S | 28 | packets/2-139_auto_interact.yaml | confirmed | (2:139) Proximity-based auto-interaction with a nearby NPC/actor. Same 28-byte structure as 2/5 but triggered automatically: InteractionType u8 @0 (always 1=proximity), SlotIndex u8 @1, ChannelByte u8 @2, reserved u8 @3, TargetActorId u32 @4, Context u8[24] @8 (zeroed). Pre-check: target must NOT be type 1001 (monster). Send size (28 bytes) is control-flow-confirmed; field VALUE semantics capture-unverified. |
| `0x2008d` | CmsgCubeGambleSubmit | C2S | 76 | packets/2-141_craft_submit.yaml | confirmed | (2:141) **RENAMED CmsgCubeGambleSubmit (2026-06-24 byte-audit, binary wins — prior CmsgCraftSubmit label and material-slots/NPC-index semantics are dropped; sole caller is the Cube-Gamble panel).** **CYCLE 15 body composition (CONSUMER-CONFIRMED):** 56-byte bet-amounts block@0 (panel+2140, 0x38 bytes), 16-byte bet-line block@0x38 (panel+2196, 0x10 bytes), 1-byte dealer-table-index@0x48 (index of nearest NPC of type 1003 with state fields +500==100 and +542==1), 3-byte alignment@0x49 = 76 bytes total. Auto-submitted on 4/100 phase-3 land when bet is pending. The Cube-Gamble panel is the sole emitter (pairs with S2C 4/99 SmsgCubeGambleResultMessage and 4/100). *(Packet-spec filename `2-141_craft_submit.yaml` retained — stale pre-re-attribution name.)* Send size (76 bytes) control-flow-confirmed; field VALUE semantics capture-unverified. |
| `0x2008e` | CmsgStorageOp | C2S | 16 | packets/2-142_storage_op.yaml | confirmed | (2:142) Storage/bank deposit-withdraw. Fixed 16-byte body: context_id u32@+0 (active storage-session reference from game-context singleton), op u8@+4 (**Op = widget_action_id − 7** — CONSUMER-CONFIRMED CYCLE 15; Op 0=action-id 7, 1=action-id 8, 2=action-id from split-amount confirm), pad 3B@+5 (not zeroed by consumer — re-impl should zero), value i64@+8 (amount/quantity; gate is value>0). Send size (16 bytes) control-flow-confirmed. Op→deposit/withdraw/rearrange binding is UNVERIFIED — pin from menu-population code or live capture. |
| `0x2008f` | CmsgPlaytimeAction | C2S | 4 | packets/2-143_playtime_action.yaml | confirmed | (2:143) **SIZE CORRECTED from var to FIXED 4 bytes.** Playtime reward panel action. Fixed 4-byte body: Mode u8 @0 (0=open/refresh, 1=claim item), SlotOrFlag u8 @1, Param1 u8 @2, Param2 u8 @3. Two callers: the panel open helper (sends 0,0,0,0) and the PlaytimePanel item claim event (sends 1, slot, param1, param2 after validating item availability and NPC proximity). Send size (4 bytes) is control-flow-confirmed; field VALUE semantics capture-unverified. Prior name 'CmsgQuestItemKeep' is SUPERSEDED. |
| `0x20091` | CmsgSkillListSubmit | C2S | var | packets/2-145_skill_list_submit.yaml | confirmed | (2:145) Skill-list submit (raised from the skill confirm panel). **Count-prefixed VARIABLE body `[u32 N][12-byte rec × N]` — the census static "16" (the prefix + one record) is CORRECTED to a count-prefixed variable list, NOT fixed-16.** Routing + the count-prefixed shape `[confirmed]`; the per-record meanings `[capture/debugger-pending]`. |
| `0x20092` | CmsgServerAckEcho | C2S | 8 | packets/2-146_server_ack_echo.yaml | confirmed | (2:146) Reactive echo-reply to S2C 5/146 server ack-request. **Fixed 8-byte body (CYCLE 4 netcode-deep re-pin: body 8 = two u32 echo words, total wire frame 16 = 8-byte header + 8-byte body).** Body: Token u32 @0 (echoed from S2C 5/146 body word 0), ServerId u32 @4 (from a persistent local server/channel identifier). REACTIVE — never sent proactively; only emitted in response to S2C 5/146. The handler validates the token against local state, shows message 51027 ('Hostile faction presence felt in this area'), and echoes the token + server id. PvP zone presence heartbeat. Send size (8 bytes) is control-flow-confirmed; field VALUE semantics capture-unverified. |
| `0x20097` | CmsgProductBuy | C2S | 1 | packets/2-151_product_buy.yaml | confirmed | (2:151) **ProductPanel's SOLE network send** (HUD-II Wave 2 recovery). A 1-byte selector body: the open/refresh path sends `0`, the tip-panel confirm path sends `200` (0xC8). Tab and page navigation are LOCAL repaints over a server-pushed recipe list (the list/money refresh arrives on S2C 3/8), NOT additional sends. The panel does **NOT** emit 2/152 -- the prior "2/152 = ProductPanel paging" attribution is a MIS-ATTRIBUTION, now RESOLVED (see the 2/152 row). **CYCLE 15 selector→reply confirmed (CONSUMER-CONFIRMED):** {0=gold-shop → S2C 3/8 SmsgShopPageUpdate (rebuilds ShopPanel; the same ShopPanel_BuildItemRow the selector-0 caller invokes); 200=cash/Diamond-shop → S2C 4/113 SmsgItemShopPurchaseResult (12-byte purchase outcome; the Diamond-domain panel that the selector-200 caller puts into state). Reply routing is server-decided; corroboration is consumer-panel pairing, not a request-stamped reply field.} The 1-byte send size and the two-value selector are control-flow-confirmed; server-side binding is consumer-corroborated (capture recommended to byte-confirm). Earlier framing tied 2/152/2/153 to this product family; 2/152 is now de-linked (it is the QuestPanel row request) -- 2/153 is retained below only where it still has independent evidence. |
| `0x20098` | CmsgQuestRowRequest | C2S | 8 | packets/2-152_quest_row_request.yaml | draft | (2:152) QuestPanel per-row "request detail / claim" round-trip: a generic two-u32 sender (8-byte body) raised by the quest-log window's list rows. **CONFLICT RESOLVED (HUD-II Wave 2): 2/152 belongs SOLELY to the QuestPanel** -- the prior "2/152 = ProductPanel paging" reading is withdrawn now that the ProductPanel recovery proved that panel sends only 2/151 (1-byte body) and never 2/152. The quest-flow VERBS (accept/proceed/give-up) ride the distinct 2/28 CmsgQuestAction; the quest-log snapshot returns on the 5/68 SmsgQuestList push channel (**and 5/73 is SmsgQuestComplete -- CYCLE 4 netcode-deep rename dropping the stale IDB autoname `SmsgGuildWarInfoUpdate`; the HUD-II Wave 4 "5/73 = GuildWarInfoUpdate" note is superseded**). The 8-byte two-u32 shape + routing are control-flow-confirmed; the per-arg VALUE meaning (quest id vs row scratch) is capture-pending. |
| `0x20099` | CmsgProductConfirm | C2S | 4 | packets/2-153_product_confirm.yaml | confirmed | (2:153) **NAME RE-ATTRIBUTED to CmsgProductConfirm (CYCLE 4 netcode-deep, the binary wins -- `CmsgPetSummon` is REFUTED).** Product/shop confirm command. **ONE builder, fixed 4-byte body; the 60-second client cooldown is real.** The pet/mount-summon reading is dropped: this build has no tamed-creature pet feature, and the single 4-byte builder is the product-confirm send. **CYCLE 15 reply opcode CONFIRMED (CONSUMER-CONFIRMED): S2C 4/79 SmsgCraftingResult (NOT 3/8 or 4/113).** The builder stamps no expected-reply field; the server unconditionally routes the commit reply through the major-4 Response table slot 1325 (4/79). 4/79 reads a 52-byte body: success flag @+8 (==1 → apply produced item); failure codes 1..5 at body@+13 → localized crafting-error strings (code 5 = server timeout, matching the 60 s client gate). Send size (4 bytes) is control-flow-confirmed; per-byte field VALUE semantics capture-unverified. |
| `0x22710` | CmsgKeepalive | C2S | 4 | packets/2-10000_keepalive.yaml | confirmed | (2:10000) Timer-armed keepalive. 4-byte zero body (a single u32 = 0). The frame template is built ONCE in the NetHandler ctor and armed at a `~20000 ms` interval (the '20' is the interval in seconds × 1000, NOT the body size). Compress-only send path — the cached buffer is LZ4-compressed once and never byte-ciphered. This is the FIRST of two keepalive-family C2S messages; the second is the on-demand 2/112 toggle (`0x20070`). Send size + body are control-flow-confirmed; on-wire cadence capture/debugger-pending. |
| `0x30001` | SmsgCharacterList | S2C | var | packets/3-1_character_list.yaml | confirmed | (3:1) Character-select list; switches to the select screen. 3-byte header [server, channel, slot mask] + per-slot 981-byte records gated by the slot bitmask. Each set-slot record = 0x370 (880) spawn descriptor + 0x60 (96) stats block + 1 selectable/occupied flag + 4-byte timing = 981 bytes. **The slot loop runs EXACTLY 5 iterations** (the account character-slot count — re-confirmed on build 263bd994; the earlier "~8 slots" reading is corrected). The inbound handler writes the 5-slot scratch the select-window then copies into the scene before building. The 981-byte record sizes (0x370/0x60/1/4) are literal in-handler constants (control-flow-confirmed); the per-field offsets *inside* the 880-byte descriptor live in the descriptor decode (structs/actor.md), not this handler. The header bytes 0/1 (ServerId/ChannelId) are read but not consumed here — their real values are capture-pending. ROLE (role confirmed on build 263bd994, Campaign 7): clears a status flag, zeroes the five-slot select scratch, then per set-mask bit reads a spawn descriptor + stats block + selectable/occupied flag + timing word into the scratch and seeds a per-slot name string, finally driving the engine scene state to the select screen. |
| `0x30004` | SmsgSceneEntityUpdate | S2C | var | packets/3-4_scene_entity_update.yaml | confirmed | (3:4) Scene / entity / char-slot update -- a VARIABLE-length slot-scratch refill / scene-clear. The minor-3 receive ladder routes minor 4 here. ROLE: loops the 5 select-scratch slots gated by SlotMask, reading per set slot a SpawnDescriptor, a StatsBlock, a selectable flag, and a timing word. Payload layout is identical to SmsgCharacterList (3/1). |
| `0x30005` | SmsgEnterGameAck | S2C | 44 | packets/3-5_enter_game_response.yaml | draft | (3:5) Enter-world acknowledgement -- the direct reply to C2S 1/9. ACCOUNT/BILLING confirm (account/char name + billing flag + account character count), NOT spawn data: it sets the client scene state to LOADING and ends char-select. The world map/position/self snapshot arrives separately on 4/1. 44-byte read = a 40-byte block + a trailing u32 char count. ROLE (role confirmed on build 263bd994, Campaign 7): reads the account/billing confirm block, drives the engine scene state to the Load value, and stores a trailing count field. The 3/5-vs-4/1 arrival ORDER is needs-capture. |
| `0x30006` | SmsgRenameCharResult | S2C | 12 | packets/3-6_rename_char_result.yaml | confirmed | (3:6) Character rename result. ROLE (role confirmed on build 263bd994, Campaign 7): clears a pending latch, reads a 12-byte block, applies the new name on success (subtype 1), and on failure shows the matching error-code message in the char-select status line; reached via the major-3 receive ladder. Size note: the 12-byte body is binary-re-confirmed (CYCLE 8, 263bd994) and refutes a prior 19-byte / embedded-name reading. Layout (body-relative): Result u8 @0 (0=fail/1=success), ErrorCode u8 @1 (failure-only, 0xC8..0xD4 -> UI message buckets), pad @0x02 (2B), then two u32 slot fields @0x04/@0x08 forwarded to the char-select slot writer on success (the success path does NO name string-copy). **CYCLE 15 error-code→message-id map (CONSUMER-CONFIRMED):** {0xC8/0xC9 → 1106; 0xCE → 1105; 0xCC/0xCD/0xCF/0xD0/0xD1/0xD2 → 1107; 0xD4 → 1113; any other → no message}. Same four message-id globals shared with 3/13/3/14/3/100. Literal CP949 text for those ids is a message-DB asset (capture-pending). Routing dispatch-table-confirmed. |
| `0x30007` | SmsgCharManageResult | S2C | 8 | packets/3-7_char_manage_result.yaml | confirmed | (3:7) Char delete/slot-pose MANAGE result -- a **Select-scene UI result only**. The 8-byte block (result u8 @0 [**1=success, 0=failure** — polarity CONFIRMED, CYCLE 15], subtype u8 @2 [**0=face-slot-back / 1=face-slot-front / 2=delete-confirmed** — CORRECTED CYCLE 15: the prior "1=rename-applied" label is REFUTED; rename rides 3/6 SmsgRenameCharResult, NOT 3/7], ready_time u32 @4 for the same-day delete cooldown; the bytes @1/@3 are read but unconsumed -> model as reserved/padding). On failure (result==0) with Subtype==0 and ReadyTime>now: formats the delete-cooldown notice in hours/minutes. The subtype block drives the char-select slot face state (0 = refresh / face the slot back, 1 = rename-applied / face the slot front, 2 = delete-confirmed -> decrements the account char-count and clears the slot), formats a same-day delete-cooldown notice from the ready-time, then resets the select scene. **It writes NO engine scene-state fields (no GameState write of any kind) -- re-confirmed Campaign 16.** The table-driven engine-state transition the earlier specs ascribed to a major-3 handler is NOT this opcode: that transitioner is 3/100 SmsgCharActionResult. 3/7 only mutates char-select UI/slot state. The minor-3 receive ladder routes minor 7 here -- re-confirmed on build 263bd994 (Campaign 10 + Campaign 16) from the dispatcher subtraction chain: 3/7 IS the manage/delete result; the variable scene-entity update is 3/4; the 16-byte enter/spawn confirm is 3/14. The local-player WORLD spawn is driven by 4/1, not 3/7. The 8-byte read and the subtype @0x02 / ready_time @0x04 offsets are control-flow-confirmed; the ready_time epoch/units stay capture-pending. **NOTE (filename mismatch -- Tier-1 to reconcile):** the linked spec is named `packets/3-4_char_manage_result.yaml` but its content is the 3/**7** char-manage result. The "3-4" in the filename is the stale pre-de-swap opcode; the file itself describes 3/7. The actual 3/4 opcode is SmsgSceneEntityUpdate (unspecced). |
| `0x30008` | SmsgShopPageUpdate | S2C | 4 | packets/3-8_shop_page_update.yaml | confirmed | (3:8) **4-byte money/gold refresh that triggers a product-panel repaint** (HUD-II Wave 2 recovery). The handler reads EXACTLY one u32 (the player's current gold/money), stores it to player-state, and refreshes the active product/service panel via the main-window service slot. It carries NO grid/page array -- the recipe list is populated from a cached vector filled elsewhere (area/NPC load), and 3/8 only re-drives the money and the repaint. The single-u32 read + the repaint trigger are control-flow-confirmed; the gold semantic is static-confirmed from the consumer. Capture-unverified on the wire. |
| `0x3000d` | SmsgCharStatusUpdate | S2C | 19 | packets/3-13_char_status_update.yaml | confirmed | (3:13) Character status update. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 19-byte body, clears a status latch, then either resets a status/text field or selects a status-text id by a sub-code and shows it on the local-player status line. Size note: the 19-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x3000e` | SmsgCharSpawnResponse | S2C | 16 | packets/3-14_char_spawn_response.yaml | confirmed | (3:14) Char enter-into-world bridge / spawn confirm -- a 16-byte block that re-enters the select-window enter-builder. The minor-3 receive ladder routes minor 14 here -- **re-confirmed on build 263bd994 (Campaign 10)**: the dispatcher subtraction chain routes minor 14 to this 16-byte spawn-confirm handler; the prior reading that 3/14 was a variable scene-entity update is SUPERSEDED -- the variable scene-entity update is 3/4, and the 8-byte manage/delete result is 3/7. NOTE: this is NOT the local-player world spawn (that is driven by 4/1). ROLE (role confirmed on build 263bd994, Campaign 7): clears an enter-flag, resets a select-window scratch, reads the 16-byte body, then on the leading flag either re-enters the enter-builder with the carried fields or starts a fallback timeout. Routing + 16-byte read size dispatch-table-confirmed; the 16-byte field VALUE semantics capture-pending. |
| `0x30015` | CmsgChatChannel | C2S | var | packets/3-21_chat_channel.yaml | draft | (3:21) Client general/channel chat: a fixed **56-byte (0x38) context header** then `[u32 textLen][text]`. **The text length prefix INCLUDES the trailing NUL** (re-confirmed on build 263bd994, Campaign 10) -- note this DIFFERS from 2/7, whose prefix excludes the NUL. The channel selector is read near the head of the header (modulo-10 of an early header byte); the text is gated under 200 chars. The 56-byte header size + length-prefixed-text + the prefix-includes-NUL rule are control-flow-confirmed; the exact channel encoding inside the header is capture-pending. |
| `0x30017` | SmsgCharStatusBytesByName | S2C | 28 | packets/3-23_char_select_status_update.yaml | confirmed | (3:23) Character status-bytes update, **keyed by NAME (CYCLE 4 netcode-deep rename; resolves the prior stale-TBD).** 28-byte body = an 8-byte header + a 17-byte NUL-bounded CP949 name @+8 + status bytes @+25/@+26. ROLE: updates a character's level/status value on the select screen by `strcmp`-matching the CP949 name string across the (<=5-slot, stride 220) roster scratch. Prior name `SmsgCharSelectStatusUpdate` is superseded. **CYCLE 8 (263bd994) re-confirm: the 17-byte CP949 name key is at BODY offset 0x08** (not 0x00/0x02; the leading 8-byte block @0x00 is copied but unused by the matcher), status byte @0x19 / level byte @0x1A, pad @0x1B. Routing + 28-byte read + the name-key are control-flow-confirmed; field VALUE semantics capture-unverified. |
| `0x30064` | SmsgCharActionResult | S2C | 4 | packets/3-100_char_action_result.yaml | confirmed | (3:100) **The table-driven engine-state transition handler** for the char/lobby flow -- THIS, not 3/7, is the major-3 message that writes the engine GameState. Reads a leading 4-byte action/result code, then a large code switch maps the code to an engine GameState write and a UI message: billing/retry codes (202/203/232) drive GameState -> 2 (Load); code 0 drives GameState -> 6 (Quit); error codes drive GameState -> 7 (Error) carrying the code as the error-detail field; the code is also passed through as a detail value and timed UI events are enqueued. The handler branches on whether a local player object is present. **Code set (re-confirmed on build 263bd994, Campaign 10): {0, 1-5, 7, 9-11, 16, 22, 23, 200-211, 220-227}, plus the specials 202/203/232. There is NO case 32** -- the earlier doc reading that listed code 32 is corrected; the real code set is broader (the 200-211 / 220-227 bands) and omits 32. Reached via the major-3 inline minor switch. **Campaign 16 correction:** the table-driven Select/InGame state-machine behaviour earlier specs attributed to 3/7 actually lives HERE at 3/100; 3/7 (SmsgCharManageResult) is a Select-scene UI result that writes no engine state. Routing + the 4-byte action-code read + the GameState write targets (2/6/7) are control-flow-confirmed. **CYCLE 15 message-id values (CONSUMER-CONFIRMED):** In-world codes 1-7 → tooltip message-ids 1201-1207 (each shows a dedicated reason string), code 22 → 1208; select-screen code 23 → 1604 (recoverable notice); generic non-{202,203,232} error → 1107. Codes {10,11,16,200-211,220-227} are publish-only (no notice). Codes {202,203,232} set GameState=2 (Load). Literal CP949 text for each message-id is a runtime message-DB asset (asset-extraction pending). |
| `0x3c350` | SmsgGmChatMessage | S2C | var | packets/3-50000_gm_chat_message.yaml | confirmed | (3:50000) GM chat / system message. ROLE (role confirmed on build 263bd994, Campaign 7): reads a length-prefixed text block, then routes it to the active chat-log window plus an auxiliary text sink (or, when out of the in-game state, to a fallback message panel) tinted with the GM colour; reached from the major-3 special dispatch arm. **HUD-II Wave 3: GM/system text routes to the chat-log windows (slots 222/163), NOT the timed notice/error modals** (a login/select fallback sink applies out-of-game). Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x40001` | SmsgGameStateTick | S2C | 9100 | packets/4-1_game_state_tick.yaml | confirmed | (4:1) Periodic game-state tick + world-entry seed -- the CARRIER of the world spawn/self-snapshot following the 1/9 -> 3/5 handshake: it builds the local player (from the slot spawn descriptor the enter handler staged), sets area/region + spawn X/Z (Y forced 0, not on wire), scenario/map-mode int, and the enter-world-ready flag, then clears keepalive-suppress. Part of the FROZEN world scene -- its per-actor batch body is OWNED elsewhere and NOT specced here. **SIZE CORRECTED (2026-06-24 byte-audit): handler reads a FIXED 9100-byte (0x238C) body; only the form-branch taken varies, not the body length. Prior "var" is withdrawn.** ROLE (role confirmed on build 263bd994, Campaign 7): reads the large world-state block; on the world-entry form it copies the actor/world tables, builds or repositions the local player, selects the map area by date/time, plays the area BGM cues, sets the enter-world-ready flag and clears keepalive-suppress. Routing dispatch-table-confirmed; field layout capture-unverified. |
| `0x40002` | SmsgGameTickConfig | S2C | 52 | packets/4-2_game_tick_config.yaml | confirmed | (4:2) Game-tick configuration. ROLE (role confirmed on build 263bd994, Campaign 7): reads a body carrying timing/scene values, pushes them into the frame-tick scheduler singleton (tick-period fields) and world-state flags, and toggles a couple of global ready flags. One of the two server messages that (re)configure tick scheduling. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x40003` | SmsgBillingInfo | S2C | var | - | confirmed | (4:3) Billing info / world-entry confirm. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 60-byte block; on the active billing flag (once, latched) it commits billing balance/time globals, performs the world-entry placement of the local player (position, area, orientation), plays the entry FX/sound, refreshes the in-game HUD, and surfaces billing-expiry notices. Size note: the 60-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x40004` | SmsgAreaEntitySnapshot | S2C | var | packets/4-4_area_entity_snapshot.yaml | confirmed | (4:4) Area entity snapshot update; carries SpawnDescriptor-family records. **SIZE CORRECTED (2026-06-24 byte-audit): packet is VARIABLE — a 17-byte header + a variable tag-record stream terminated by tag 0. Per-tag record sizes: {1/2/3 → 892; 4 → 24; 6 → 36; 9 → 24}. Prior size column "892" was the tag-1/2/3 actor-record size, NOT the packet size; the 4-4 YAML correctly says variable.** The prefix for each tag-1/2/3 record: id-key u32 @+0, variant byte @+4 (gates ==5 visual), field byte @+5; the spawn factory copies exactly 0x370 = 880 bytes for the descriptor core (= 8-byte prefix + 880-byte SpawnDescriptor core + 4-byte trailer = 892 per actor record). ROLE (role confirmed on build 263bd994, Campaign 7): streams an area snapshot -- a header then a typed-record loop (spawn/refresh/move/name/relation cases) that creates, places and updates actors in the active area from SpawnDescriptor-family records. Routing + per-record sizes are control-flow-confirmed; the descriptor interior field VALUE semantics live in structs/actor.md, capture-unverified. |
| `0x40005` | SmsgItemUseResult | S2C | 44 | packets/4-5_item_use_result.yaml | confirmed | (4:5) Item-use result. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 44-byte block; the failure path surfaces item-kind-specific messaging (cooldown/time-remaining formatting via preset string ids) keyed on the item category, and the success path (when the actor is the local player) plays the use FX/sound and spawns the item-use world effect, with category bands selecting effect and sound ids. Routing dispatch-table-confirmed; 44-byte body layout confirmed. |
| `0x4000c` | SmsgEquipItemResult | S2C | 16 | packets/4-12_equip_item_result.yaml | confirmed | (4:12) Equip-item result. ROLE (role confirmed on build 263bd994, Campaign 7): reads a fixed block; on the success subtype it applies the equip change to the local player (with a special-slot test) and either refreshes the local player or routes through a follow-up panel; on the failure result it surfaces an error via the message UI. Routing dispatch-table-confirmed; 16-byte body layout confirmed. |
| `0x4000d` | SmsgLocalPlayerStateSync | S2C | 56 | packets/4-13_local_player_state_sync.yaml | confirmed | (4:13) Local player state sync. Handler is mislabelled in raw notes with a stale Push/5-100 suffix; it is wired as Response 4/13. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 56-byte block, resolves the actor and proceeds only when it is the local player, then writes movement/orientation and a state byte into the local-player descriptor, snaps the actor when the position delta is large, and fires a nearby-portal travel check. Size note: the 56-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x4000e` | SmsgGroundItemSlotAck | S2C | 20 | packets/4-14_ground_item_drop_ack.yaml | confirmed | (4:14) Ground-item slot / drop acknowledgement. Reply to the C2S 2/14 drop request; result gate + source-slot descriptor that spawns the dropped item. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 20-byte block; on result 0 it closes a panel, on subtype 1 it applies the source-slot descriptor that spawns the dropped item via the world-item manager. Routing dispatch-table-confirmed; 20-byte body layout capture-unverified. |
| `0x4000f` | SmsgItemWorldPickupAck | S2C | 36 | packets/4-15_item_world_pickup_ack.yaml | confirmed | (4:15) World item pickup acknowledgement. Reply to the C2S 2/15 pickup request; result + success-subtype selector (100=share, 101=plain insert, else=insert+despawn) + item id. ROLE (role confirmed on build 263bd994, Campaign 7): branches on the result/subtype, resolves the picked item id, formats pickup/notice text, and plays the coin sound for the coin item id. **CYCLE 15 outcome selector (CONSUMER-CONFIRMED):** result@+8 (0=failure/nonzero=success). FAILURE: subtype@+9 {1→msg 10001, 2→10002, 3→10003, 4→10004, 5→10005 (level/eligibility); 0/other→close panels, no message}. SUCCESS: subtype@+9 {100=client-resolved free-slot insert+register (slot_param@+0x10); 101=plain insert; else=insert+world-entity despawn by world_key@+0x0C}. world_key@+0x0C (u32) CONFIRMED = ground-item world-entity removal key (else path). item_id@+0x18 (u32), count@+0x1C (i32). Routing dispatch-table-confirmed; per-field VALUE semantics capture-unverified. |
| `0x40010` | SmsgEquipChangeResult | S2C | 20 | packets/4-16_equip_change_result.yaml | confirmed | (4:16) Equip change result. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 20-byte block; on success refreshes the local player's equipment-slot mirror (20 x 16-byte records), re-evaluates equipped-item visuals, and triggers an appearance/weapon refresh or a guild-aware re-skin; on failure clears the busy flag. Routing dispatch-table-confirmed; 20-byte body layout confirmed. |
| `0x40011` | SmsgQuickEquipSlotAck | S2C | 16 | packets/4-17_quick_equip_slot_ack.yaml | confirmed | (4:17) Quick-equip slot acknowledgement. ROLE (role confirmed on build 263bd994, Campaign 7): reads a small fixed result block, decrements a slot counter past a threshold, and on success applies the quick-equip change, else shows a generic result; no actor lookup. Routing dispatch-table-confirmed; 16-byte body layout confirmed. |
| `0x40013` | SmsgNpcBuyOrAcquireAck | S2C | 56 | packets/4-19_npc_buy_or_acquire_ack.yaml | confirmed | (4:19) NPC buy / inventory acquire acknowledgement. **HUD-II Wave 3 (build 263bd994): 0x38 (56)-byte read** -- acquired-item record @0x00 (16B), result @0x10, sub-flag @0x11, time/duration trailer beyond. Reply to C2S 2/19 (and storefront 2/115). ROLE (role confirmed on build 263bd994, Campaign 7): on success applies the acquired item to the NPC panel; on the alternate result formats item duration / time-remaining text from preset string templates into the panel. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x40014` | SmsgNpcSellItemAck | S2C | 24 | packets/4-20_npc_sell_item_ack.yaml | confirmed | (4:20) NPC sell-item acknowledgement. **HUD-II Wave 3 (build 263bd994): 0x18 (24)-byte read** -- entity key @0x04, result @0x08, sub-flag @0x0a. Reply to C2S 2/20. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 0x18 (24)-byte block, on success applies the sell result to the NPC-shop panel and, when the seller is the local player, copies the refreshed inventory block; otherwise dismisses a pending notice. Size note: the 24-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x40015` | SmsgNpcShopSlotClearAck | S2C | 12 | packets/4-21_npc_shop_slot_clear_ack.yaml | confirmed | (4:21) NPC shop slot clear acknowledgement. **HUD-II Wave 3 (build 263bd994): 0xC (12)-byte read** -- slot descriptor @0x00 (8B), result word @0x08. ROLE (role confirmed on build 263bd994, Campaign 7): reads a thin 12-byte block and, by result byte, either applies a slot change to the NPC shop panel or clears it. Size note: the 12-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x40016` | SmsgItemSlotStateAck | S2C | 36 | packets/4-22_item_slot_state_ack.yaml | confirmed | (4:22) Item slot state acknowledgement. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 36-byte block; on result==0 dismisses a pending notice, on result==1 applies the item-slot state update. Routing dispatch-table-confirmed; 36-byte body layout confirmed. |
| `0x40017` | SmsgUserTradeRequestResult | S2C | 20 | packets/4-23_trade_request_result.yaml | confirmed | (4:23) User trade request result. **NOTES CORRECTED (2026-06-24 byte-audit, binary wins — the prior layout 'phase byte @10 {0=cancel,2=incoming,3=accepted}, requester @4, target @12, responder @16' is REFUTED).** Handler reads ONLY Selector byte @body+8 (0=decline, 1=accept) and, on the decline path, Reason byte @body+9 (reasons 1..5 each map to a decline-reason string). No phase byte at +10; the actor-id fields live inside the forwarded 20-byte block and are read downstream by the window opener, not at fixed offsets in this handler. The 4-23 YAML banner is already corrected. Pairs with C2S 2/23. ROLE: drives the player-to-player trade-request state machine: accept selector opens the trade window; decline path tears down the window and maps the reason byte to a decline-reason string. Routing + 20-byte read + selector@8/reason@9 are control-flow-confirmed; field VALUE semantics capture-unverified. |
| `0x40018` | SmsgUserTradeSlotUpdate | S2C | 44 | packets/4-24_trade_slot_update.yaml | confirmed | (4:24) User trade slot update -- the INCREMENTAL single-slot populate. Fixed 44-byte (0x2C) record: result @0, category @0x0A (0xFF=money), item id/key @0x0C, destination slot index @0x24 (0..59), owner actor id @0x28 (SIDE DISCRIMINATOR -- == local player id => my side, else their side). The owner-id field is what populates the partner half of the trade window. ROLE (role confirmed on build 263bd994, Campaign 7): reads a trade slot record and applies it to the trade panel, with a clear/reset path on the negative result and a refresh on the alternate subtype. Routing + 44-byte read + the named offsets static-confirmed; field VALUE semantics capture-unverified. |
| `0x40019` | SmsgUserTradeFullResponse | S2C | var | packets/4-25_trade_full_response.yaml | confirmed | (4:25) Full user-trade response -- the BULK full-refresh populate. 28-byte (0x1C) header (result @0, bulk subtype @8, record count @9) + Count x 16-byte slot records (Count capped at 40 by the apply loop). ROLE (role confirmed on build 263bd994, Campaign 7): reads the trade header and applies it to the trade window; on the bulk subtype it loops the declared count reading fixed-size slot records and folding each into the local trade-slot table; on the cancel result it surfaces a notice. Header size + record stride (16B) + 40-cap static-confirmed; field VALUE semantics + the per-record field roles capture-unverified. |
| `0x4001c` | SmsgRespawnConfirm | S2C | var | - | confirmed | (4:28) Respawn confirmation. ROLE (role confirmed on build 263bd994, Campaign 7): reads a fixed block; on one subtype sets a respawn flag and refreshes UI, on another relocates the local player (position/heading) into the area and rebinds it. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x4001d` | SmsgStatUpdate | S2C | 36 | packets/4-29_stat_update.yaml | draft | (4:29) Stat-allocation ack: result byte + five u32 stat echoes + remaining points (36-byte fixed block). Pairs with C2S 2/29. ROLE (role confirmed on build 263bd994, Campaign 7): on result-OK writes the echoed stat values into their globals and refreshes the stat window. Routing dispatch-table-confirmed; field layout capture-unverified. |
| `0x4001e` | SmsgSocialPanelTarget | S2C | var | - | confirmed | (4:30) Social panel target. ROLE (role confirmed on build 263bd994, Campaign 7): clears a UI latch, reads a 20-byte block and opens the social/preview target window from it. Size note: the 20-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x40023` | SmsgPartyInviteState | S2C | var | - | confirmed | (4:35) Party invite state. ROLE (role confirmed on build 263bd994, Campaign 7): clears a shared UI panel, reads a 56-byte block, and hands it to the party/social subsystem. Size note: the 56-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x40024` | SmsgPartyMemberRemoveResult | S2C | 56 | packets/4-36_party_member_remove_result.yaml | confirmed | (4:36) Party member remove result: submode @10 (0=member left, 1=expelled), requester @4, removed-member id @52 (submode 0) or @12 (submode 1), 8-entry u32 roster array @20 (56-byte fixed read). Pairs with C2S 2/36. ROLE (role confirmed on build 263bd994, Campaign 7): clears a pending notice, reads the 56-byte block, and on the success submode applies the party roster removal. Routing dispatch-table-confirmed; field layout capture-unverified. |
| `0x40025` | SmsgPartyLeaderActionResult | S2C | var | - | confirmed | (4:37) Party leader action result. ROLE (role confirmed on build 263bd994, Campaign 7): clears a pending notice, reads a 56-byte block, and on success applies the leader-action result to the party subsystem. Size note: the 56-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x40027` | SmsgResponseDiscard39 | S2C | var | - | confirmed | (4:39) Thin discard/drain slot; semantics uncertain. ROLE (role confirmed on build 263bd994, Campaign 7): zeroes a 32-byte scratch, reads the payload into it, and returns without consuming it -- a genuine drain/no-op handler. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x40028` | SmsgMotionWeaponFxSwap | S2C | 164 | - | confirmed | (4:40) **RENAMED SmsgMotionWeaponFxSwap (2026-06-24 byte-audit — installer autoname SmsgResponseSlot40; descriptive name matches behaviour).** Local-player motion/weapon-FX swap record. **SIZE PINNED to a FIXED 164 bytes (0xA4) (CYCLE 4 netcode-deep, the binary wins -- the "var thin slot" reading is corrected; this is a medium record, not a thin slot).** ROLE (role confirmed on build 263bd994, Campaign 7): reads the 164-byte block, drives a local-player visual/motion swap by the result byte, and forwards the block to an NPC-panel updater. Routing + the 164-byte read are control-flow-confirmed; field VALUE layout not yet specced. |
| `0x40029` | SmsgSkillHotbarAssignResult | S2C | var | - | confirmed | (4:41) Skill hotbar assign result. ROLE (role confirmed on build 263bd994, Campaign 7): on success applies the hotbar assignment to the skill panel; on failure clears the affected quick-slot entry and surfaces a reason string selected from a preset table by the reason byte. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x4002a` | SmsgPlayerInteractionState | S2C | var | - | confirmed | (4:42) Player interaction state. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 56-byte body and forwards it to the social/interaction-state apply routine. Size note: the 56-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x4002b` | SmsgPlayerInteractionResult | S2C | var | - | confirmed | (4:43) Player interaction result. ROLE (role confirmed on build 263bd994, Campaign 7): reads a fixed block and a global mode gate; formats one of several interaction outcome strings, plays cues, looks up the two participant actors, and on the active branch swaps their motion/weapon visuals and posts a colored system message. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x4002c` | SmsgActorTickTableOpA | S2C | 16 | packets/4-44_actor_tick_table_op_a.yaml | confirmed | (4:44) Actor tick-table op A. ROLE (role confirmed on build 263bd994, Campaign 7): branches on an op selector and, on the table-load path, copies a fixed-length per-actor record table from the local-player object into a global tick table. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x4002d` | SmsgActorTickTableOpB | S2C | 16 | packets/4-45_actor_tick_table_op_b.yaml | confirmed | (4:45) Actor tick-table op B. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 16-byte block and, on a leading op byte, either resets a shared UI panel (op 0) or applies the block to the actor tick-table subsystem (op 1). Size note: the 16-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x4002e` | SmsgActorTickTableOpC | S2C | 16 | packets/4-46_actor_tick_table_op_c.yaml | confirmed | (4:46) Actor tick-table op C. ROLE (role confirmed on build 263bd994, Campaign 7): on result==0 dismisses a pending notice, on result==1 applies the 16-byte op to the actor tick-table subsystem. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x4002f` | SmsgSocialAckDrain | S2C | var | - | confirmed | (4:47) Social acknowledgement drain. ROLE (role confirmed on build 263bd994, Campaign 7): dismisses a pending UI notice and reads (drains) a 16-byte block with no further state mutation. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x40030` | SmsgRankProgressEvent | S2C | 236 | - | confirmed | (4:48) Rank progress event. **SIZE PINNED to a FIXED 236 bytes (0xEC) (CYCLE 4 netcode-deep): a 12-byte header + 8 x 28-byte records (record base @+0x0C, stride 28, count 8 => 12 + 8*28 = 236 exactly). The earlier "12-byte overrun" worry is REFUTED -- the handler reads exactly 236 and the 12-byte header was double-counted.** ROLE (role confirmed on build 263bd994, Campaign 7): clears a UI busy flag, reads the 236-byte block, and hands it to the rank-progress subsystem singleton. Routing + the 236-byte read (12 + 8x28) are control-flow-confirmed; field VALUE layout not yet specced. |
| `0x40031` | SmsgRankProgressPanelUpdate | S2C | var | - | confirmed | (4:49) Rank progress panel update. ROLE (role confirmed on build 263bd994, Campaign 7): clears a UI message latch, reads a 32-byte block, applies it through the shared rank-progress helper, then refreshes the rank/progress panel. Size note: the 32-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x40032` | SmsgUpgradeItemResult | S2C | 32 | packets/4-50_upgrade_item_result.yaml | confirmed | (4:50) Item upgrade result. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 32-byte block; on the success/fail byte it swaps the local player's motion + weapon FX to the success or failure animation, then applies the result block to the inventory/upgrade panel. Routing dispatch-table-confirmed; 32-byte body layout confirmed. |
| `0x40036` | SmsgGuildRankSlotUpdate | S2C | var | - | confirmed | (4:54) Guild rank slot update. ROLE (role confirmed on build 263bd994, Campaign 7): primes the guild singleton, clears a panel slot, reads a 44-byte block, and on the set subtype forwards the block to the guild rank-slot apply routine. Size note: the 44-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x40037` | SmsgGuildInfoUpdateResult | S2C | var | - | confirmed | (4:55) Guild info update result. ROLE (role confirmed on build 263bd994, Campaign 7): reads a fixed block plus a short string and, by a mode byte, assigns or clears a guild-name/info string on the addressed actor and refreshes its guild display. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x40038` | SmsgSceneEnterSpawnSnapshot | S2C | 1552 | - | confirmed | (4:56) **STRUCTURED PANEL snapshot -- RECLASSIFIED out of the thin-slot family (CYCLE 4 netcode-deep, the binary wins).** FIXED 1552 bytes (0x610): a 16-byte header (subtype @+8, actor-id @+0xC) + transform array A (64 x 16 @+0x10) + transform array B (64 x 8 @+0x410) -- fills 1552 exactly (array B base = +260 dwords = +0x410). Apply path = scene/spawn population. ROLE (role confirmed on build 263bd994, Campaign 7): reads the 1552-byte record into a pre-initialised scratch and applies the structured header + dual transform arrays (A: 64 × 16 @+0x10, B: 64 × 8 @+0x410) down the scene/spawn-population path, keyed on the subtype @+8 and actor-id @+0xC. Routing + the 1552-byte structured layout are control-flow-confirmed; field VALUE semantics not yet specced. |
| `0x40039` | SmsgResponseSlot57 | S2C | var | - | confirmed | (4:57) Thin UI-slot updater; semantics uncertain. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 0x1C (28)-byte block, on success applies the slot update to its UI panel, otherwise dismisses a pending notice. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x4003a` | SmsgResponseSlot58 | S2C | var | - | confirmed | (4:58) Thin UI-slot updater; semantics uncertain. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 0x34 (52)-byte block, on success applies the slot update to its UI panel, otherwise dismisses a pending notice, raises message 10009, and resets the related panel/list state. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x4003c` | SmsgResponseSlot60 | S2C | var | - | confirmed | (4:60) Thin UI-slot updater; semantics uncertain. ROLE (role confirmed on build 263bd994, Campaign 7): clears a UI panel, reads a fixed block, and only when a gate byte is set primes a singleton and forwards the block to an apply routine. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x4003d` | SmsgGuildStateChangeResult | S2C | 52 | - | confirmed | (4:61) Guild state change result. ROLE (role confirmed on build 263bd994, Campaign 7): reads a **FIXED 52-byte body** (literal field-reader size, CONSUMER-CONFIRMED CYCLE 15), branches on a gate byte @+8 to drive guild-state apply or feedback notices; posts coloured guild notices and a cooldown-days line. **CYCLE 15 key layout facts (CONSUMER-CONFIRMED):** gate@+8 (non-zero=apply-branch/zero=feedback-branch); result@+9; action@+10 (0=leave, 1=join/create, 2=gold-update, 3..5=feedback-only); grade@+11; GuildName 16B CP949@+12 (NUL forced at +28); GuildId u16@+40; MoneyBalance64 i64@+44. Cooldown (action 0/result 2): attribute-slot-93 value read in minutes / 1440 = displayed days (delta, NOT epoch). Action×result matrix selects guild-panel UI list entries or notice ids 21147/21148. Routing dispatch-table-confirmed; field VALUE semantics capture-unverified. |
| `0x4003e` | SmsgGuildInviteJoinState | S2C | var | - | confirmed | (4:62) Guild invite/join state. ROLE (role confirmed on build 263bd994, Campaign 7): clears a UI busy flag, reads an 80-byte block, and hands it to the guild subsystem singleton to update invite/join state. Size note: the 80-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x4003f` | SmsgGuildMemberRemoveResult | S2C | var | - | confirmed | (4:63) Guild member remove result. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 52-byte body; on the non-success subtype it maps a reason code to a guild message-string id (the 21098..21149 / 53008 guild range) and shows it as a system notice, otherwise it applies a guild-roster update. Size note: the 52-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x40040` | SmsgGuildPositionChangeResult | S2C | var | - | confirmed | (4:64) Guild position change result. ROLE (role confirmed on build 263bd994, Campaign 7): clears a pending notice, reads a 20-byte block, and on the apply subtype (gate byte at +8 == 1) forwards it to a guild-state sub-handler, otherwise (result byte at +9 == 2) shows guild notice message 21135. Size note: the 20-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x40041` | SmsgGuildInfoFullSync | S2C | 1812 | packets/4-65_guild_info_full_sync.yaml | confirmed | (4:65) Guild info full sync. **CONFIRMED 1812-byte (0x714) block (CYCLE 15 re-confirmed).** Layout = a ~60-byte header (subtype gate @8, guild name @0x0A CP949 char[18], guild id @0x1C, rank byte @0x1E, two header quantities @0x20/@0x24, fame/points i64 @0x2C, war flag @0x34) + FIVE parallel 50-entry per-member arrays (ids @0x3C i32[50], status bytes @0x104 u8[50], 17-byte CP949 names @0x136, class bytes @0x47C u8[50], three dword arrays @0x4BC/@0x584/@0x64C i32[50] ending exactly at 1812). **REFUTES the "50 x 36-byte member record" guess** -- per-member data is STRIPED across parallel arrays, not packed per member. ROLE (role confirmed on build 263bd994, Campaign 7): gate byte @+8 value 1 → full-sync path (applies the 50-member arrays); any other value → **leave path, but ONLY when the guild-state leave-pending flag is set** — then shows leave notice (MessageDB id 10011, amber), sets local-player neutral-state byte at actor+172 to value 2, and if the embedded exp-refund quantity (i64) is positive shows the refund notice (id 2183) and subtracts it from the local exp counters; finally clears the guild-state global, guild-id word, and guild-name string, and re-applies motion. Block size + header/array layout static-confirmed (CYCLE 15); the two header quantities (@0x20/@0x24 capacity vs points) and two per-member dword arrays (@0x4BC/@0x64C contribution/last-login) are UNVERIFIED. |
| `0x40042` | SmsgResponseSlot66 | S2C | var | - | confirmed | (4:66) Thin UI-slot updater; semantics uncertain. ROLE (role confirmed on build 263bd994, Campaign 7): resets a UI panel, reads a fixed block, drives a local-player motion swap on the success byte, then forwards the block to a panel updater. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x40046` | SmsgDeliveryRecord | S2C | 140 | - | confirmed | (4:70) **Delivery / mail INBOX record -- ATTRIBUTED (CYCLE 7): the reply to C2S 2/71 CmsgDeliveryClaim that returns one delivery-box record.** ROLE (role confirmed on build 263bd994, Campaign 7): reads a fixed 140-byte block, on result==1 applies it to the delivery/inbox subsystem, otherwise raises notice/error code 40042. Part of the mail/delivery family (compose-send 2/70, claim 2/71, arrival notice 1/20). Routing + the 140-byte read are control-flow-confirmed; the record field layout + the success/fail polarity are capture/debugger-pending. |
| `0x40047` | SmsgRelationPartyPanelSnapshot | S2C | 1092 | - | confirmed | (4:71) **RENAMED SmsgRelationPartyPanelSnapshot (2026-06-24 byte-audit — installer autoname SmsgResponseSlot71; descriptive name promoted: structured 8-slot relation/party panel snapshot, NOT a thin slot).** **STRUCTURED PANEL snapshot -- RECLASSIFIED out of the thin-slot family (CYCLE 4 netcode-deep, the binary wins).** FIXED 1092 bytes (0x444): a header (subtype @+8) + an id array (8 x 4 @+0xC) + a status array (8 x 1 @+0x2C) + mirror-relative 80-byte slot records + 17-byte name cells. (The nearby 1556/1552 / 1096/1092 immediates are read-sizes, NOT string ids.) ROLE (role confirmed on build 263bd994, Campaign 7): reads the 1092-byte structured block into a stack buffer and forwards it to the panel consumer. Routing + the 1092-byte structured layout are control-flow-confirmed; the deep mirror-relative sub-tables + field VALUE semantics not yet specced. |
| `0x40048` | SmsgResponseSlot72 | S2C | var | - | confirmed | (4:72) Thin UI-slot updater; semantics uncertain. ROLE (role confirmed on build 263bd994, Campaign 7): reads a fixed block; when the gate byte is set it forwards two float values to a rank/progress panel-slot consumer. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x4004a` | SmsgStallListRefill | S2C | var | packets/4-74_stall_list_refill.yaml | confirmed | (4:74) **HUD-II Wave 4 re-attribution: the personal-stall (player-vendor) market LIST REFILL** -- the reply to C2S 2/74 CmsgStallListRequest that rebuilds the StallListPanel (slot 228) row list. Reads a leading clear/append flag plus N fixed 36-byte records, with the record count derived from the frame size as N = (frameSize - 14) / 36; each record = {u32 stall id @0, u8 availability flag @4, 31-byte CP949 "stallName`ownerActorId" string @5}. The prior "thin UI-slot updater, semantics uncertain" label is superseded. Routing + the 36-byte stride + the count formula are control-flow-confirmed; the per-field record VALUE meanings (availability flag bits, string parse) are capture/debugger-pending. |
| `0x4004b` | SmsgProductPurchaseResultPanel | S2C | 184 | - | confirmed | (4:75) **RENAMED SmsgProductPurchaseResultPanel (2026-06-24 byte-audit — installer autoname SmsgResponseSlot75; descriptive name matches behaviour: product/cash purchase result panel). SIZE CORRECTED var→184 (handler reads a fixed 0xB8 = 184-byte body).** Product/cash purchase result panel. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 184-byte result block gated by a success byte; on success applies it to a UI view, on failure branches by a subtype byte (101 = show error string, 102 = decrement account character count) and forwards the result to a panel updater. Routing + 184-byte read are control-flow-confirmed; field layout not yet specced. |
| `0x4004c` | SmsgPartyAcceptResult | S2C | var | - | confirmed | (4:76) Party accept result. ROLE (role confirmed on build 263bd994, Campaign 7): clears a pending notice, reads a 52-byte block, and on success runs the paired-actor join sequence (face-each-other, motion swap, accept SFX) else raises notice 10068; a submode also drives a couple/pair flow. Size note: the 52-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x4004e` | SmsgRentalItemExpirySweep | S2C | 12 | - | confirmed | (4:78) **RENAMED SmsgRentalItemExpirySweep (2026-06-24 byte-audit, binary wins — prior SmsgServerTimeNotification name and "global/local time setter" role are REFUTED). SIZE CORRECTED var→12 (handler reads a fixed 12-byte body).** The verified handler feeds the +0x08 u32 into Inventory_SweepExpiredRentalItems (rental-item expiry sweep), NOT a time setter. ROLE: reads a fixed 12-byte body and triggers a rental-item expiry sweep keyed on the +0x08 u32 value. Routing + 12-byte read are control-flow-confirmed; field layout not yet specced. |
| `0x4004f` | SmsgCraftingResult | S2C | var | - | confirmed | (4:79) Crafting result. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 0x34 (52)-byte block, on success applies the crafting result to the production panel, otherwise shows the matching production-error message (localized when the VFS is mounted, English fallback otherwise) and resets the production state. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x40050` | SmsgPvpDeathResult | S2C | 80 | - | confirmed | (4:80) PvP death result. **SIZE CORRECTED (2026-06-24 byte-audit): var→80 (handler reads fixed 0x50 bytes).** ROLE (role confirmed on build 263bd994, Campaign 7): reads a fixed 80-byte block; on the non-self branch updates the slain actor and selects one of a family of localized PvP death/kill notice strings by a subtype byte; the self branch runs a local death effect. Routing + 80-byte read are control-flow-confirmed; field layout not yet specced. |
| `0x40051` | SmsgActionErrorResult | S2C | var | packets/4-81_guild_diplomacy_result.yaml | confirmed | (4:81) **GENERIC ACTION-ERROR RESULT -- RE-ATTRIBUTED (CYCLE 4 netcode-deep, the binary wins; the prior "SmsgGuildDiplomacyResult" reading is DROPPED).** *(Packet-spec filename retained; content = SmsgActionErrorResult -- the `4-81_guild_diplomacy_result.yaml` name is the stale pre-re-attribution label, NOT renamed; the yaml body describes the generic action-error result.)* This slot is NOT the guild-diplomacy reply: **the guild-diplomacy verdict rides 4/61 SmsgGuildStateChangeResult (52-byte body), not 4/81.** Body layout: actionContext u8[8]@+0..+7 (consumed only when status==1), status@+8, errorCode@+9, seconds@+10. status==1: apply actor-visual from the +0 context block; errorCode 100→message 51026. **CYCLE 15 error-code→message-id map FULLY RESOLVED (CONSUMER-CONFIRMED):** 0xFF→21078; 0x01→21074; 0x02→21075; 0x03→21076; 0x04→21077; 0x05→2163; 0x06→21097; 0x07→2164; 0x08→2165; 0x09→2166; 0x0A→2167; 0x0B→2168; 0x0C→2169 (ALSO triggers actor-visual side-effect); 0x0D→2170; 0x0E→2171; 0x0F→51016; 0x10→51017; 0x11→51018+51019; 0x12→51020; 0x13→51023; 0x14→51019; 0x15→51024/51025 (mm:ss: seconds@+10/60=minutes, %60=secs; 51024 if minutes>0, else 51025); 0x16→no message; 0x17→51029+client-config-table percent (record 100, float@record+4 — NOT a wire byte); 0x18→51033; other→no message. ROLE: clears a UI message latch and surfaces the mapped error/notice via chat-log + system notice (colour 0xFFFF8000). (The C2S 2/81 CmsgGuildDiplomacyDeclare therefore pairs with 4/61, not this slot.) Routing + all offsets are control-flow-confirmed; literal CP949 text capture-pending. |
| `0x40052` | SmsgBillingBalanceUpdate | S2C | var | - | confirmed | (4:82) Billing balance update. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 16-byte body, on one sub-code applies a points delta and posts the gained-points notice, on others routes billing-panel updates, then refreshes the billing UI (distinct from the normal-gold channel 4/108). Size note: the 16-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x40053` | SmsgBillingItemUseResult | S2C | var | - | confirmed | (4:83) Billing item-use result. ROLE (role confirmed on build 263bd994, Campaign 7): branches on result/subtype to surface billing item-use messaging and arm a billing timer. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x40054` | SmsgRecordSlotConsumeResult | S2C | var | - | confirmed | (4:84) Record slot consume/clear result. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 12-byte body; one branch resets the relation/guild/buddy HUD panels and decrements (or clears) a per-slot record counter, the other branch refreshes a tracked HUD panel by a selector. Size note: the 12-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x4005d` | SmsgUserVoteTallyUpdate | S2C | var | - | confirmed | (4:93) User vote tally update. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 16-byte body and forwards it to the vote/event subsystem object for tally application; payload semantics capture-unverified. Size note: the 16-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x4005f` | SmsgLocalPlayerBattleMode | S2C | var | - | confirmed | (4:95) Local player battle-mode update. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 12-byte block and stores one byte (at payload +10) into the local-player object's battle-mode field. Size note: the 12-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x40060` | SmsgActorGuildRosterEntry | S2C | var | - | confirmed | (4:96) Actor guild roster entry update. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 52-byte block, resolves the actor by id, writes its guild-tag and guild-state fields plus mirrors the local player's guild-name buffer, refreshes the social/roster panel, and posts a 'joined guild' system notice (format string 2184). Size note: the 52-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x40061` | SmsgAreaSkillEffectPanel | S2C | var | - | confirmed | (4:97) Area skill-effect result panel update. ROLE (role confirmed on build 263bd994, Campaign 7): clears a UI latch, reads a fixed block and forwards it verbatim to the area skill-effect result panel updater. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x40063` | SmsgCubeGambleResultMessage | S2C | 16 | - | confirmed | (4:99) **Cube-Gamble (cube-bet) minigame RESULT panel -- RE-LABELLED (CYCLE 7, the binary wins; the prior `SmsgCombatResultMessage` label is DROPPED).** This is the result message of the wager/reel daily-bet minigame (panel `data/ui/cubegamble.dds`, submitted via C2S 2/141), NOT combat. **CYCLE 15 body layout (CONSUMER-CONFIRMED):** leading 8B@+0x00 not consumed; sub-kind@+0x08 (1=bet-echo route, else=result-announce route); result-code@+0x09; bet-type@+0x0A (bet-echo only: 15-18=bet-slot-0..3, 100=chip-buy); wager@+0x0C u32 (bet-echo only). BET-ECHO: deducts/credits the panel's running stake, plays sound 2. RESULT-ANNOUNCE: codes 1..8 → message-DB ids {1→58001+58002; 2→58003+58004; 3→58005+payout+58006; 4→58007+payout+58008; 5→58009; 6→58010; 7→58011+58006; 8→58030}; codes 3/4 payout = panel-internal config-table lookup (not a wire field). Pairs with 4/100. Routing + 16-byte read are control-flow-confirmed; wire VALUE semantics capture-unverified. *spec:* handlers.md §4/99, combat.md. |
| `0x40064` | SmsgCubeGambleReelUpdate | S2C | 188 | - | confirmed | (4:100) **Cube-Gamble (cube-bet) REEL UPDATE -- RE-LABELLED (CYCLE 7, the binary wins; the prior `SmsgCombatAttackUpdate` label is DROPPED).** Drives the gamble REEL UI state, NOT combat. **CYCLE 15 body layout (CONSUMER-CONFIRMED):** leading 8B@+0x00 not consumed; phase@+0x08 {5=spin-start/settle, 4=intermediate-advance, 3=land/reveal}; per-spin sub-kind@+0x09 (stored at panel+2212; ==1 selects alternate win-indicator set); reel-digit-pack@+0x0A (two BCD-style dice tens/ones, 0..9 each; 0xFF=reset-sentinel); animation-value u32@+0x0C; settled-money u64@+0x10 (lo@+0x10, hi@+0x14 — win/loss=signed-delta vs old money); reel-history 5B@+0x18; reel-index 5B@+0x1D; board-state block 152B@+0x24 (memcpy'd to panel+1200). Phase 3 with pending bet auto-submits C2S 2/141. Win/loss: delta=newMoney-oldMoney (>0=win/msg 58017, <0=loss, ==0=push). Pairs with 4/99. Routing + 188-byte read are control-flow-confirmed; wire VALUE semantics capture-unverified. *spec:* handlers.md §4/100, combat.md. |
| `0x40065` | SmsgGlobalScalarPairUpdate | S2C | var | - | confirmed | (4:101) Global scalar-pair update. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 16-byte block and conditionally writes two global scalar values from its two trailing dwords. Size note: the 16-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x40066` | SmsgSkillWindowStateUpdate | S2C | 476 | packets/4-102_buff_state.yaml | confirmed | (4:102) **Skill/state-WINDOW snapshot -- this is NOT the buff-slot channel (CYCLE 7 clarification, the binary wins).** The buff/status-slot delta is **S2C 5/31 SmsgBuffSlotUpdate (56B)**, which SUPERSEDES the older "buff = 4/102" label; 4/102 is the full skill/state-window block. The 476-byte (0x1DC) body = a 116-byte player stat block + 30 fixed 12-byte active-buff records starting at payload +116 (the window also re-paints its embedded buff bar from those records, but the authoritative per-slot buff push is 5/31, not this opcode). One opcode rebuilds the whole skill/state window (the stale raw 'quest data update' label is wrong). ROLE (role confirmed on build 263bd994, Campaign 7): reads the 476-byte block (player stat block + fixed active-buff records) and rebuilds the skill/state window and its buff bar. Routing + the 476-byte read are control-flow-confirmed; field layout capture-unverified. |
| `0x40067` | SmsgGuildPanelTextUpdate | S2C | 204 | - | confirmed | (4:103) Guild panel text update. **SIZE CORRECTED (2026-06-24 byte-audit): var→204 (handler reads fixed 0xCC = 204 bytes).** ROLE (role confirmed on build 263bd994, Campaign 7): reads a 204-byte block and forwards it to the guild panel sub-object held in a main-window service slot. Routing + 204-byte read are control-flow-confirmed; field layout not yet specced. |
| `0x40069` | SmsgGagePanelProgressUpdate | S2C | var | - | confirmed | (4:105) Gage panel progress update. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 12-byte block and writes the current progress value into the gage panel, resetting it at zero and firing the panel's completion callback once the value reaches its target. Size note: the 12-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x4006b` | SmsgEventLotteryPickResult | S2C | var | - | confirmed | (4:107) Event lottery number-pick result. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 28-byte block and forwards the carried pick value/record to the event-lottery panel object held in a MainHandler service slot. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x4006c` | SmsgPlayerGoldBalanceUpdate | S2C | 16 | packets/4-108_player_gold_balance_update.yaml | confirmed | (4:108) Player gold balance update (the NORMAL gold channel, distinct from billing/cash 4/82). ROLE (role confirmed on build 263bd994, Campaign 7): reads a 16-byte body, writes the new gold value onto the local player, and refreshes the money/inventory panel. Routing dispatch-table-confirmed; 16-byte body layout confirmed. |
| `0x4006d` | SmsgLocalActorSkillStateFlag | S2C | 12 | - | confirmed | (4:109) Local actor skill-state flag. **SIZE CORRECTED (2026-06-24 byte-audit): var→12 (handler reads fixed 0x0C bytes).** ROLE (role confirmed on build 263bd994, Campaign 7): reads a 12-byte body, accepts it only when the carried id is the local player, writes a per-player skill-state byte, then refreshes the skill-state panel. Routing + 12-byte read are control-flow-confirmed; field layout not yet specced. |
| `0x40071` | SmsgItemShopPurchaseResult | S2C | 12 | packets/4-113_item_shop_purchase_result.yaml | confirmed | (4:113) Item-shop purchase result. **HUD-II Wave 3 (build 263bd994): 0xC (12)-byte read** -- success @0x08 (string 65008), result_code @0x09 (codes 0..4 -> 65006/65007/65010/65011/65012). Surfaced via the global notice sink (ErrorPanel slot 168). DISTINCT from the cash shop 4/114. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 12-byte body; on the success flag it shows the purchase-success notice, otherwise it maps the result code to one of several item-shop error message strings and shows it. Size note: the 12-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x40072` | SmsgCashShopActionResult | S2C | 12 | packets/4-114_cash_shop_action_result.yaml | confirmed | (4:114) Cash-shop action result -- the CASH (billing-currency) shop, SEPARATE from the gold item-shop 4/113. **HUD-II Wave 3 (build 263bd994): 0xC (12)-byte read** -- result code @0x08 (1..4 -> 54127..54130). ROLE (role confirmed on build 263bd994, Campaign 7): reads a 12-byte block and maps a result code (1-4) to cash-shop message ids 54127-54130, shown as a system notice. Size note: the 12-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x40073` | SmsgItemShopBalanceUpdate | S2C | 24 | packets/4-115_item_shop_balance_update.yaml | confirmed | (4:115) Item-shop balance update -- the NPC VENDOR money refresh. **HUD-II Wave 3 (build 263bd994): 0x18 (24)-byte read** -- balance A @0x00, balance B @0x04, success @0x08, fail_code @0x09 (45020..45024), balance C @0x0c. The vendor money channel is THIS opcode, NOT the crafting product panel's 3/8 (4-byte gold) -- they are different opcodes/panels. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 24-byte body; on success it writes two balance fields on the local player and refreshes the item-shop panel total, otherwise it surfaces a failure code. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x40078` | SmsgActorTableBatchResult | S2C | var | - | confirmed | (4:120) Actor table batch result. ROLE (role confirmed on build 263bd994, Campaign 7): guards the empty/no-payload case, then forwards the payload body (frame +8 onward) to an actor-table batch consumer held in a MainHandler service slot. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x4007a` | SmsgResponseSlot122 | S2C | var | - | confirmed | (4:122) Thin UI-slot updater; semantics uncertain. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 16-byte block, on success forwards it to a UI list/slot sink (special-casing one subtype with message 67013), otherwise raises a notice/error code. Size note: the 16-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x4007b` | SmsgResponseSlot123 | S2C | var | - | confirmed | (4:123) Thin UI-slot updater; semantics uncertain. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 12-byte body, refreshes a service-panel widget, and on a flag byte either updates a panel slot by index or posts a generic notice string. Size note: the 12-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x4007d` | SmsgResponseSlot125 | S2C | var | - | confirmed | (4:125) Thin UI-slot updater; semantics uncertain. ROLE (role confirmed on build 263bd994, Campaign 7): pulses a refresh on a single main-window service-slot panel; no payload read. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x4007e` | SmsgFactionSideAssign | S2C | var | - | confirmed | (4:126) **Faction / brood-war SIDE assignment -- ATTRIBUTED (CYCLE 7): a server Push carrying a single faction/side byte (the brood-war side assignment); there is no matching C2S request.** ROLE (role confirmed on build 263bd994, Campaign 7): reads a 12-byte block, on one branch sets a global side/toggle byte and refreshes a dependent actor/panel, otherwise raises a notice/error code on a service-slot panel. Size note: the 12-byte read is the handler's bulk-read minimum, not a proven frame size. Routing + the 12-byte read are control-flow-confirmed; the side-byte VALUE map is capture/debugger-pending. |
| `0x40084` | SmsgGmNoticeError | S2C | var | - | confirmed | (4:132) GM notice / error. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 12-byte block, looks up an error string by (sub-code + 70000), dismisses the active modal via a singleton vtable call, and posts the error text to the message UI. Size note: the 12-byte read is the handler's bulk-read minimum, not a proven frame size. **HUD-II Wave 3: routes to the ErrorPanel timed notice (slot 168) via the global notice sink** (which delegates to AnnouncePanel slot 221), in addition to dismissing the active modal first. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x40085` | SmsgRankProgressUpdate | S2C | var | - | confirmed | (4:133) Rank progress update. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 12-byte block; a sentinel value of -1 shows error message 71003, otherwise the carried value is passed to the rank/progress setter. Size note: the 12-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x40086` | SmsgStatChangeNotify | S2C | var | - | confirmed | (4:134) Stat change notify. ROLE (role confirmed on build 263bd994, Campaign 7): gates on the local-player key, then shows one of a contiguous family of localized stat-change notice strings selected by a code byte. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x40087` | SmsgResponseSlot135 | S2C | var | - | confirmed | (4:135) Thin UI-slot updater; semantics uncertain. ROLE (role confirmed on build 263bd994, Campaign 7): reads an 80-byte body, accepts it only when the carried id is the local player, then forwards several fields to a HUD/panel slot updater (exact panel not disambiguated). Size note: the 80-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x40089` | SmsgGatheringResult | S2C | var | - | confirmed | (4:137) Gathering result. ROLE (role confirmed on build 263bd994, Campaign 7): reads a fixed block gated on the local-player id and, on a subtype selector, opens or closes the gathering panel and updates the targeted resource-node state (toggling node depletion and refreshing the world), with a shared finalize path. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x4008a` | SmsgNoticeError | S2C | var | - | confirmed | (4:138) Notice / error. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 12-byte body, gates on the addressed actor being the local player, then switches on a code byte to select a notice string (party-relationship cases resolve a member by group id) and posts it to the chat log. Size note: the 12-byte read is the handler's bulk-read minimum, not a proven frame size. **HUD-II Wave 3: posts to the chat log and the ErrorPanel timed notice (slot 168) via the global notice sink** (delegating to AnnouncePanel slot 221). Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x4008b` | SmsgItemUseEffect | S2C | 24 | - | confirmed | (4:139) Item-use effect. **WIRE READ PINNED to a FIXED 24 bytes (CYCLE 4 netcode-deep, the binary wins -- the "var" tail was a mis-read: the variable 28-byte block the handler FORWARDS is a downstream echo, NOT a wire field).** ROLE (role confirmed on build 263bd994, Campaign 7): reads the 24-byte header, requires the actor id @+4 to be the local player, resolves the slot to an item id, then spawns the effect + 3D SFX variant chosen by item-id ranges (213060037.., 213060091-213060104, 213060109.., and 213062504-213062577) and forwards a 28-byte echo to an item-use sub-handler. Routing + the 24-byte read are control-flow-confirmed; field VALUE layout not yet specced. |
| `0x4008c` | SmsgColoredSystemText | S2C | var | - | confirmed | (4:140) Coloured system text. ROLE (role confirmed on build 263bd994, Campaign 7): reads a fixed body, maps the colour-code byte to an ARGB colour via a switch, and posts the trailing CP949 string to the chat/system log in that colour. **HUD-II Wave 3: posts to the chat log and the ErrorPanel timed notice (slot 168) via the global notice sink** (delegating to AnnouncePanel slot 221). Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x4008e` | SmsgResponseSlot142 | S2C | var | - | confirmed | (4:142) Thin UI-slot updater; semantics uncertain. ROLE (role confirmed on build 263bd994, Campaign 7): reads a fixed block gated on the local-player id; on the OK result it stores four globals and refreshes three panels, otherwise it surfaces a preset error. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x4008f` | SmsgTrackedItemPanelToggle | S2C | var | - | observed | (4:143) NEW (Campaign 7 build 263bd994; not in prior catalog). One physical handler serves the adjacent Response minors 143 and 144 (it branches on the frame's minor). For minor 143: reads a 16-byte block and, gated on the local-player id, toggles a tracked panel widget (show/hide) and refreshes its slot. Size note: the 16-byte read is the handler's bulk-read minimum, not a proven frame size. Semantics inferred, capture-unverified -- listed `observed` pending an analyst dispatch-table cross-check that names minor 143 distinctly. See also 4/144. Field layout not yet specced. |
| `0x40090` | SmsgTrackedItemRecordFold | S2C | var | - | observed | (4:144) NEW (Campaign 7 build 263bd994; not in prior catalog). Served by the same physical handler as 4/143 (branches on the minor). For minor 144: reads a 36-byte block and, on a gate byte, folds a 16-byte tracked-item record into a global tracked-item table, formats an item-name notice (string id 10006), and refreshes the panel slot. Size note: the 36-byte read is the handler's bulk-read minimum, not a proven frame size. Semantics inferred, capture-unverified -- listed `observed` pending an analyst dispatch-table cross-check. See also 4/143. Field layout not yet specced. |
| `0x40092` | SmsgShowMessage51027 | S2C | var | - | confirmed | (4:146) Show message (string id 51027). ROLE (role confirmed on build 263bd994, Campaign 7): resolves preset string id 51027 and posts it to the message UI with a fixed colour. **HUD-II Wave 3: routes to the ErrorPanel timed notice (slot 168) via the global notice sink** (delegating to AnnouncePanel slot 221). Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x40094` | SmsgPlaytimeRewardResult | S2C | var | - | confirmed | (4:148) Playtime reward result. ROLE (role confirmed on build 263bd994, Campaign 7): on the success branch plays a 2D cue, applies a reward update and forwards a value; on the failure branch a reason selector maps to one of several preset result strings shown via the message UI. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x40095` | SmsgItemPanelSlotChunk | S2C | var | packets/4-149_item_panel_slot_chunk.yaml | confirmed | (4:149) Item panel slot chunk. ROLE (role confirmed on build 263bd994, Campaign 7): gates on a kind byte and the local-player id key, then copies a run of 16-byte item-slot records from the payload into one of two fixed slot arrays (bag vs equip family) at a base index, bounded by a slot count and the array end. Routing dispatch-table-confirmed; variable length layout confirmed. |
| `0x40096` | SmsgSkillPointUpdate | S2C | var | packets/4-150_skill_point_update.yaml | confirmed | (4:150) Skill-point update: result byte + local-player id key + mode (1=set total, 2=skill level-up notice) + value, at fixed offsets 0/4/8/12. Modelled as a 16-byte block; true total length capture-unverified. ROLE (role confirmed on build 263bd994, Campaign 7): gated on a success byte and a local-player id match; a mode selector either sets the local player's total skill-point field or, on a skill level-up notice, formats one of two messages from string-table ids and posts it to the UI. Routing dispatch-table-confirmed; field layout capture-unverified. |
| `0x40097` | SmsgResponseSlot151 | S2C | var | - | confirmed | (4:151) Thin UI-slot updater; semantics uncertain. ROLE (role confirmed on build 263bd994, Campaign 7): reads three trailing dwords from the payload and forwards them to a UI panel via the runtime-singleton table. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x40098` | SmsgResponseSlot152 | S2C | var | - | confirmed | (4:152) Thin UI-slot updater; semantics uncertain. ROLE (role confirmed on build 263bd994, Campaign 7): validates the target against the local player id and, for two specific result codes, surfaces a preset message; otherwise no-ops. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x40099` | SmsgItemPanelSlotRefresh | S2C | var | packets/4-153_item_panel_slot_refresh.yaml | confirmed | (4:153) Item panel slot refresh. ROLE (role confirmed on build 263bd994, Campaign 7): gates on the local-player key, then either closes the item panel, no-ops, or refreshes an item panel slot from the carried record. Routing dispatch-table-confirmed; variable length layout confirmed. |
| `0x401f4` | SmsgShowPopupByCode | S2C | 4 | packets/4-500_show_popup_by_code.yaml | confirmed | (4:500) Special: routed outside the response table; shows a preset popup by code. **HUD-II Wave 3 (build 263bd994): the handler reads a single u32 popupCode @0; codes 1..7 each select one of seven preset popup strings**, shown as a timed on-screen notice via ErrorPanel (slot 168, default 5000 ms), which delegates to the scrolling AnnouncePanel (slot 221) when present. The 4-byte read + routing are control-flow-confirmed; the seven code->string values are capture-pending. |
| `0x4c350` | SmsgResponseDiscardText | S2C | var | - | confirmed | (4:50000) Special: routed outside the response table; discards a text payload. ROLE (role confirmed on build 263bd994, Campaign 7): zeroes a 120-byte scratch, reads the payload into it, and discards it -- a drain/no-op text sink. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x50000` | SmsgCharDespawn | S2C | 12 | packets/5-0_char_despawn.yaml | confirmed | (5:0) Actor leaves the world. 12-byte fixed block. ROLE (role confirmed on build 263bd994, Campaign 7): looks the actor up by (id,sort), and for a non-local actor with the notify flag set spawns a despawn effect and pushes an 'X left' notice, releases the actor's tracked render handle, then removes the actor from the world manager. |
| `0x50001` | SmsgActorSpawnExtended | S2C | 912 | packets/5-1_actor_spawn_extended.yaml | confirmed | (5:1) Extended actor spawn (player/mob/item): 12-byte prefix + 880-byte SpawnDescriptor + 20-byte trailer. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 912-byte block whose leading byte is a sort selector (1 player / 2 mob / 3 NPC), then resolves/creates the actor, copies the descriptor and CP949 name, seeds position and heading, and triggers spawn FX/sound (the 912-byte read matches this size exactly). Routing dispatch-table-confirmed; field layout capture-unverified. |
| `0x50003` | SmsgCharSpawn | S2C | 908 | packets/5-3_char_spawn.yaml | confirmed | (5:3) Actor enters the world; payload is a SpawnDescriptor record. 908-byte fixed block. ROLE (role confirmed on build 263bd994, Campaign 7): reads the ~908-byte SpawnDescriptor block, resolves/creates the actor by (id,sort), copies the descriptor and name, seeds position/heading, and triggers the spawn effect (different effect ids for self vs others). |
| `0x50005` | SmsgActorStateEvent | S2C | 32 | packets/5-5_actor_state_event.yaml | confirmed | (5:5) Actor state event. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 32-byte event block, resolves a source/event actor and a target actor (composite key), and drives state-keyed visual effects and 3D SFX. The dispatch key is NOT in this payload — it is the **source actor's `inner_event_code` (u16 at actor+0x88 = SpawnDescriptor+0x14)**, assigned at spawn time. **CYCLE 15 `inner_event_code` value meanings (CONSUMER-CONFIRMED):** {1001=spawn world-positioned effect (id from src+0x208) at target pos/rot + context sound selected from src+0x34 id; 1011=disguise/polymorph apply to target using src+0x208 as effect id + play sound 800000009; 1020=rename target — copy 17-byte CP949 name from body@+0x0C into target name field; 1021=motion/appearance change — write low-16 of src+0x208 to target+0xA2 then reset/re-apply target motion (tag 800000009); 1023=same world-effect as 1001 but fixed sound id 863000001; 1041=set target in-combat flag (actor+0x3A0 = 1)}. Dispatch precedence: on-hit-item-id/on-hit-MP-item-id on the source actor pre-empts codes 1011/1020/1021 (triggers effect 350000021/350000022 instead); codes 1001/1023/1041 are unconditional. Payload layout (32 bytes): target_sort u32@+0x00, target_id u32@+0x04, source_id u32@+0x08, name_buf char[20]@+0x0C (17-byte CP949 used by code 1020). Routing dispatch-table-confirmed; 32-byte body layout CONSUMER-CONFIRMED (CYCLE 15). |
| `0x50006` | SmsgActorAutotargetOrMotion | S2C | var | - | confirmed | (5:6) Actor auto-target / motion. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 16-byte block, resolves the target actor by id via the actor manager, then on a one-byte mode either applies attack-mode and refreshes joint FX (mode 1) or changes the actor's motion and swaps its weapon FX (other non-zero), or clears attack mode (mode 0). Size note: the 16-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x50007` | SmsgChatBroadcast | S2C | var | packets/5-7_chat_broadcast.yaml | draft | (5:7) Chat broadcast: 36-byte header + variable text body. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 36-byte header (channel byte near the header tail) then the text body, resolves the speaking actor, and dispatches on the channel code: say/shout/party/guild/alliance/event/special each format from a preset string template (say uses string id 17003) into the chat log with the channel colour, whisper channels (6/7) route through the whisper-display path, and a band of high channel codes drives system/notice lines. |
| `0x50009` | SmsgExpGain | S2C | 32 | packets/5-9_exp_gain.yaml | confirmed | (5:9) Experience gain/loss: recipient sort/id + XP-source sort/id + signed i64 XP amount + two proficiency slots (32-byte block). Adds the i64 amount to the running XP accumulator; spawns exp-orb FX and a +xp/-xp float (UI strings 10085/10086). ROLE (role confirmed on build 263bd994, Campaign 7): resolves recipient and XP-source actors, accumulates a signed 64-bit XP amount into the local-player running total (splitting out a bonus portion), updates proficiency slots, and floats a +xp/-xp UI message plus exp-orb visual effects (380001xxx / 380002xxx family). Routing dispatch-table-confirmed; field layout capture-unverified. |
| `0x5000a` | SmsgCharDeath | S2C | 20 | packets/5-10_combat_death.yaml | confirmed | (5:10) Character death. **SIZE CORRECTED (2026-06-24 byte-audit): var→20 (handler reads a fixed 20-byte body; matches YAML 5-10).** ROLE (role confirmed on build 263bd994, Campaign 7): reads a 20-byte body, resolves the dying actor, clears its combat/buff state and active-buff slots, plays the death FX and (for the local player) the death/respawn UI messages, sets the scene death state, and enqueues the respawn-countdown timed event. Routing + 20-byte read are control-flow-confirmed; field layout specced (see packets/5-10_combat_death.yaml). |
| `0x5000b` | SmsgRankXpGain | S2C | 20 | packets/5-11_rank_xp_gain.yaml | confirmed | (5:11) Rank/honor XP gain (20-byte block): recipient id/sort + u64 amount + mode byte. Separate progression channel from 5/9 character XP; no HP/MP or level math. ROLE (role confirmed on build 263bd994, Campaign 7): resolves the actor and proceeds only for the local player, then formats the gained amount (applying a server-config penalty split when set) into a '+xp'/level-up floating message and notice; a 300/5 state branch instead writes the raw amount into a progress field. Routing dispatch-table-confirmed; field layout capture-unverified. |
| `0x5000c` | SmsgActorVisualSlotSet | S2C | 20 | packets/5-12_actor_visual_slot_set.yaml | confirmed | (5:12) Actor visual slot set. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 20-byte body, looks up the actor, writes a visual attachment id/value into the indexed equipment-visual slot, sets weapon/shield visibility flags for special slots, and rebuilds the actor visual. Size note: the 20-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x5000d` | SmsgActorMovementUpdate | S2C | 40 | packets/5-13_actor_movement_update.yaml | confirmed | (5:13) Actor movement update. 40-byte fixed block (XZ-plane, float coords). ROLE (role confirmed on build 263bd994, Campaign 7): reads a 40-byte block (XZ-plane float coords + heading + mode/run flags), resolves the actor, and drives its movement interpolation/snap -- choosing between teleport-snap, run/walk interp, and face-target paths based on the mode byte and squared XZ distance thresholds. |
| `0x5000e` | SmsgCombatEffectInstanceSpawn | S2C | 48 | packets/5-14_ground_item_spawn.yaml | confirmed | (5:14) Combat-effect / ground-item spawn push (its primary role is the live "item dropped in the world" push). Shares a 48-byte wire shape with combat-effect instances, so two regions are unread by the ground-item path. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 48-byte body, spawns a world item/effect instance at the given position (coin id substituted when flagged), optionally clears an actor visual slot, plays selected pickup/effect SFX by item-id ranges, and posts an inventory-full notice. Routing dispatch-table-confirmed; 48-byte body layout capture-unverified. |
| `0x5000f` | SmsgTrackedWorldObjectRemove | S2C | 16 | packets/5-15_ground_item_remove.yaml | confirmed | (5:15) Tracked world-object remove (ground-item despawn after a pickup); carries the tracked id to remove + a notify flag for an "X picked up Y" notice. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 16-byte body, removes the named tracked ground-item entity, and (excluding the coin item id and when the remover is another player) shows an 'X picked up Y' notice via message string 10018. Routing dispatch-table-confirmed; 16-byte body layout capture-unverified. |
| `0x50010` | SmsgActorVisualSlotClear | S2C | 16 | packets/5-16_actor_visual_slot_clear.yaml | confirmed | (5:16) Actor visual slot clear. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 16-byte body (actor key + mode byte + slot index), looks up the actor, scans its 20-entry visual-slot array (16-byte stride), clears the indexed slot, and rebuilds the actor visual (mode 0 clears+rebuilds, mode 1 clears one slot -- mirror of 5/12). Size note: the 16-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x50012` | SmsgGameClockUpdate | S2C | var | - | confirmed | (5:18) Game clock update. ROLE (role confirmed on build 263bd994, Campaign 7): reads an 8-byte two-dword body and applies it to the game-clock/schedule object obtained from a runtime singleton. Size note: the 8-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x50015` | SmsgPartyRosterEvent | S2C | 12 | packets/5-21_party_event.yaml | confirmed | (5:21) Party roster event: event code @8 (0=join, 1=leave, 2=update, 3=disband), affected member slot @9 (12-byte fixed read). ROLE (role confirmed on build 263bd994, Campaign 7): reads a 12-byte body, resolves the actor, and only when it is the local player applies the carried party roster event (join/leave/update/disband) to the party UI. Routing dispatch-table-confirmed; field layout capture-unverified. |
| `0x5001a` | SmsgLocalPlayerRelationSlot | S2C | var | - | confirmed | (5:26) Local player relation slot update. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 28-byte body, resolves the actor by composite key, and only when it is the local player updates a relation/friend slot panel. **CANDIDATE inbound populate for the FriendPanel friend list / online-status (UNVERIFIED, debugger/capture-pending)** -- best static match for the per-friend slot update fed by C2S 2/49 / 2/54, but NOT confirmed to write the friend-window records; do NOT implement against it as the friend feed until a capture or dispatch cross-check confirms. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x5001c` | SmsgRespawnAtPoint | S2C | var | - | confirmed | (5:28) Respawn at point. ROLE (role confirmed on build 263bd994, Campaign 7): reads an actor id plus position, recreates the actor, and restores its position/orientation/state fields at the respawn point. BUILD DELTA: prior handlers.md notes inferred the position was taken from the client's own cached transform with only the id on the wire; the build-263bd994 read instead carries position in the body. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x5001f` | SmsgBuffSlotUpdate | S2C | 56 | - | confirmed | (5:31) Buff slot update. **SIZE CORRECTED (2026-06-24 byte-audit): var→56 (handler reads a fixed 56-byte body; matches YAML 5-31).** ROLE (role confirmed on build 263bd994, Campaign 7): reads a 56-byte body, resolves the target actor, and writes (or clears) a fixed-stride active-buff record into the actor's buff-slot array indexed by the carried slot, releasing and re-ticking the actor buff slots; the local-player branch also mirrors the record into the global buff-bar tables and refreshes the buff/state panels. Routing + 56-byte read are control-flow-confirmed; field layout not yet specced. |
| `0x50020` | SmsgLevelUp | S2C | 48 | packets/5-32_level_up.yaml | confirmed | (5:32) Level up: new level + packed HP/MP + stamina + rank-XP tail (48-byte fixed block). ROLE (role confirmed on build 263bd994, Campaign 7): reads a 48-byte block, applies the new level plus packed HP/MP/stamina to the actor, plays level-up FX and (for the local player) the level-up UI/sound. **HUD-II Wave 3: this is the ONLY confirmed handler that opens MessagePanel (slot 190) -- at new level 12/24 it opens msginfo.do codes 100/101 -- but that is a CLIENT-derived level milestone, NOT a wire message-code field.** (5/32 is already catalogued; the earlier 'catalogue gap' note is stale.) Routing dispatch-table-confirmed; field layout capture-unverified. |
| `0x50021` | SmsgSkillHotbarSlotSet | S2C | var | - | confirmed | (5:33) Skill hotbar slot set. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 20-byte block, resolves the actor by composite key and requires it to be the local player, then (slot index < 240) writes an 8-byte hotbar entry {value, points} at stride 8*slot into the hotbar tables. Size note: the 20-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x50022` | SmsgBillingBannerToggle | S2C | var | - | confirmed | (5:34) Billing banner toggle. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 4-byte value and shows the billing banner widget (fading it in) when the flag is 1 or hides it when 0. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x50026` | SmsgPartyMemberStats | S2C | var | - | confirmed | (5:38) Party member stats. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 100-byte body and applies the carried member identity/name and stat block to the party-member panel object. Size note: the 100-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x50027` | SmsgPushNop39 | S2C | var | - | confirmed | (5:39) No-op slot. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 12-byte block off the payload and discards it with no side effect (confirmed no-op). Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x5002a` | SmsgPlayerPairSystemNotice | S2C | var | - | confirmed | (5:42) Player-pair system notice. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 16-byte body carrying two actor ids, resolves both player actors, formats a paired-name system message from a string template, and posts it to the system message UI in a fixed colour. **HUD-II Wave 3: a sibling of the couple/pair-relation family alongside 5/53 (the PetPanel/couple populate) and 5/64** (remote actor relation pair). Size note: the 16-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x50033` | SmsgSkillGuideState | S2C | var | - | confirmed | (5:51) Skill guide state. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 24-byte block, proceeds only when the carried id matches the local player, sets a skill-guide flag byte when a sub-code is in range (value-2 <= 3), and applies the guide state. Size note: the 24-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x50034` | SmsgActorSkillAction | S2C | var | packets/5-52_actor_skill_action.yaml | confirmed | (5:52) Actor skill action / combat result. 24-byte (0x18) header + count@header+0x16 × 36-byte (0x24) per-target records (count capped at 40). ROLE (role confirmed on build 263bd994, Campaign 7): reads a 24-byte header then up to N fixed-size per-target records, resolves the acting actor (including the basic-melee slot marker and the dash/skill paths), applies HP/stamina deltas and combat state to the local player, spawns the combat effect, and formats a damage-total notice. **CYCLE 15 per-target record layout (CONSUMER-CONFIRMED, 36 bytes per record):** sub-key u8@+0x00 (actor composite key — sort half); key u32@+0x04 (actor composite key — id half); +0x08 4B opaque (bulk-forwarded, not field-read); +0x0C 4B opaque (bulk-forwarded; re-copied to clone+40 in AoE fan-out); +0x10 4B opaque (NOT read by this handler — not damage, not action code; bulk-copied only); damage-lo u32@+0x14 (low dword of signed i64 damage accumulator); damage-hi u32@+0x18 (high dword, added via carry); +0x1C..+0x23 8B opaque tail (not read). The i64 at +0x14/+0x18 is summed across records, negated, and — when caster is local player AND combat-log option is ON — displayed as a thousands-grouped total via MessageDB string id 2212 with the TargetCount. Absolute per-target HP is NOT in this record; it arrives on 5/53 SmsgActorVitalsAndPairState. Literal value semantics (HP-damage vs scaled magnitude, actor-id encoding) remain capture-pending (R-CAP: breakpoint at record bulk-read to observe the i64 value and actor-key vs known target). |
| `0x50035` | SmsgActorVitalsAndPairState | S2C | 32 | packets/5-53_actor_vitals_and_pair_state.yaml | confirmed | (5:53) Actor vitals (HP as one i64/stamina/third vital) + pair-relation state (32-byte fixed block). ROLE (role confirmed on build 263bd994, Campaign 7): writes HP/stamina/third-vital onto the looked-up actor (clamping the local player against the stat caps), and on the pair (couple) sub-mode (sort==2) forms or breaks a partner link and posts the paired/unpaired notice. **HUD-II Wave 3: this is the data feed that auto-shows the player-couple/pair companion window (the IDB-named `PetPanel`, MainWindow slot 194) -- the panel populates the partnered actor's name/level/relation gauges from this 32-byte block and the 10025..10032 couple notice strings; there is no tamed-creature pet feature in this build.** **CYCLE 15 field layout (CONSUMER-CONFIRMED, 32 bytes):** sort u8@+0x00 (actor category; value 8 remapped to 1 before lookup; sort==2 gates the pair-relation broadcast path); pad 3B@+0x01; actor_id u32@+0x04; gap 2B@+0x08; relation_state u8@+0x0A (pair-relation state → actor SD+0x38: 0=no relation, 1=formed/active, 2=second state; transition: old==0→new==1 = FORMED, old∈{1,2}→new==0 = BROKEN); relation_state_2 u8@+0x0B (→ actor SD+0x39; mirrored to local global for local player); partner_actor_id u32@+0x0C (stored at actor+0x3C8; looked up as sort-1 PC; cleared on BROKEN); current_hp lo-u32@+0x10 + hi-u32@+0x14 (**one signed i64** — see C15-S12: three independent proofs; summed-then-sign-guarded as a pair, not two u32s); current_mp i32@+0x18 (independent signed i32 clamp); stamina i32@+0x1C (written to local global only when target=local player). Pair-relation human label (couple/sworn-pair/etc.) is in MessageDB ids 10025-10032 (CP949 asset, not in binary). Routing dispatch-table-confirmed; VALUE semantics (literal HP magnitude, actor-key encoding) capture-pending. |
| `0x50037` | SmsgGuildNameDisplayUpdate | S2C | var | - | confirmed | (5:55) Guild name display update. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 40-byte block, resolves the actor by (id,sort), and on a mode byte writes the guild-name string onto the actor and refreshes the guild-name overlay; the set/clear paths additionally repaint the guild panel name/text fields when the actor is the local player's guild. Size note: the 40-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x50039` | SmsgTrackedPanelSlotUpdate | S2C | 24 | packets/5-57_tracked_panel_slot_update.yaml | confirmed | (5:57) Tracked panel slot update. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 24-byte block and forwards it to the tracked-panel sub-handler. Routing dispatch-table-confirmed; 24-byte body layout confirmed. |
| `0x5003b` | SmsgRankProgressSfxEvent | S2C | 76 | - | confirmed | (5:59) Rank progress SFX event. **BODY SIZE confirmed 76 bytes (0x4C) on CYCLE 14 re-anchor (build f61f66a9; updated from "var").** ROLE (role confirmed on build 263bd994, Campaign 7): reads a 76-byte fixed block, suppresses a UI element, then fires a rank-progress sound/FX event. Routing dispatch-table-confirmed; field layout not yet specced. **IDB label note (build f61f66a9, CYCLE 14): the handler function's IDB name was lost in the names.yaml rebuild for this build; the function is currently unnamed in the live IDB. Push slot minor 59 (table index 1459) routing is unchanged. Catalog name SmsgRankProgressSfxEvent remains correct; a restore rename is pending (ida-toolsmith).** |
| `0x5003d` | SmsgActorNameOverlaySet | S2C | var | - | confirmed | (5:61) Actor name overlay set. ROLE (role confirmed on build 263bd994, Campaign 7): reads an 84-byte block, resolves the actor by id, and assigns its overlay name string. Size note: the 84-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x50040` | SmsgRemoteActorRelationPair | S2C | var | - | confirmed | (5:64) Remote actor relation pair update. ROLE (role confirmed on build 263bd994, Campaign 7): reads two actor ids, looks both up, and writes a relation byte on each (one side a code carried in the payload, the other a fixed code), skipping when either is the local player or already paired. **HUD-II Wave 3: part of the couple/pair-relation family alongside 5/53 (the local-player couple populate -> PetPanel slot 194) and 5/42** (player-pair system notice). Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x50041` | SmsgGuildMemberRosterUpdate | S2C | var | - | confirmed | (5:65) Guild member roster update. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 32-byte block, resolves the actor by id, and either assigns the member's guild-name string + rank/flag fields or clears them; also mirrors the change onto the local player when it is the affected member. **CYCLE 4 netcode-deep flags this as var->fixed (the handler does a fixed 32-byte read); the precise wire frame size is not yet pinned in the inputs, so the size column stays `var` pending a frame-size confirmation.** Size note: the 32-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x50043` | SmsgStatsUpdate | S2C | 36 | packets/5-67_stats_update.yaml | confirmed | (5:67) Authoritative stats resync (36-byte block): the five primary stats + current XP. World-entry sync that re-primes the exp bar. id/name/direction(S2C)/size(36) correct. **OFFSET LAYOUT DRIFT NOTE (2026-06-24 byte-audit — YAML field offsets need spec-author correction):** CurrentXp i64 is at body offset **0x10..0x17** (NOT 0x1C); the five stat values sit at offsets **0x08, 0x0C, 0x18, 0x1C, 0x20** surrounding the XP qword (NOT five contiguous u32 at 0x08..0x18). The 'stat channels {0,5,2,6}' note is accurate for the four channel pushes; a fifth value at 0x1C goes only to the actor, not a channel. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 36-byte block and resyncs the five primary stats plus current XP onto the addressed actor and, when it is the local player, re-primes the stat/exp UI. Routing + 36-byte read are control-flow-confirmed. |
| `0x50044` | SmsgQuestList | S2C | 452 | packets/5-68_quest_list.yaml | confirmed | (5:68) Quest-log snapshot: fixed 452-byte block the client copies into its quest mirror to rebuild the three quest-log tabs. id/name/direction(S2C)/size(452) correct. **FIELD CORRECTIONS (2026-06-24 byte-audit — YAML value corrections needed): quest entry count is 20 (NOT 10); the panel-open gate flag is at body offset +0x09 (NOT +0x08).** Size + routing confirmed; internal byte layout UNDECODED (reserved opaque), capture-required. ROLE (role confirmed on build 263bd994, Campaign 7): reads the fixed 452-byte block, snapshots a quest-state flag, copies the block into the client quest mirror, then on a flag transition opens or closes the quest-log panel (playing the open cue on open). |
| `0x50049` | SmsgQuestComplete | S2C | 344 | packets/5-73_quest_complete.yaml | confirmed | (5:73) **RENAMED to SmsgQuestComplete (CYCLE 4 netcode-deep, the binary wins -- the installed IDB autoname `SmsgGuildWarInfoUpdate` is STALE; the install slot is real, the name is not).** The Push installer wires this slot (confirmed at the installer this pass); the handler reads a fixed 344-byte (0x158) block (a row of up to 10 entries, each an item/icon + a 17-byte name + a numeric value) and drives the quest-result panel. The prior HUD-II Wave 4 "GuildWarInfoUpdate -- NOT quest-complete" reversal is itself SUPERSEDED by the CYCLE 4 completeness register (W4.5 + §2.5), which re-confirms 5/73 = SmsgQuestComplete. Routing + the fixed 344-byte read are control-flow-confirmed; the internal body layout is UNDECODED (reserved opaque), capture-required. **NAME `[UNVERIFIED]` residual:** the 10-row name+number table body shape is compatible with an info/ranking-table reading too -- the static body shape alone does not fully settle the name; owned by the names lane, do not re-point on static body shape alone. |
| `0x5004c` | SmsgPartyMemberJoined | S2C | var | - | confirmed | (5:76) Party member joined. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 36-byte body and resolves an actor; on one sub-code it swaps the target's motion and weapon FX and plays a paired-actor cue when close to the local player, on another it applies an actor-to-actor interaction with a magnitude derived from level difference, and when the carried name equals the local player it refreshes a roster/relation panel record. Body behaviour leans toward a player-pair/relation interaction event; the canonical name is retained from dispatch routing and is not capture-confirmed. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x5004d` | SmsgRankProgressPanelBulk | S2C | var | - | confirmed | (5:77) Rank progress panel bulk. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 400-byte block, looks up the addressed player actor, and on the local player applies it through the shared rank-progress helper then refreshes the panel. Size note: the 400-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x5004f` | SmsgActorDeathState | S2C | 20 | - | confirmed | (5:79) Actor death state. ROLE (role confirmed on build 263bd994, Campaign 7): looks the dying actor up by key and, on a DeathOp byte, performs death-visual setup, spawns a death effect, sets a death sub-state, or clears/revives. **CYCLE 15 body layout (CONSUMER-CONFIRMED, FIXED 20 bytes):** leading dword@+0x00 not consumed (serial/echo); dying-actor key u32@+0x04 (actor lookup kind=1; also effect anchor in mode 1); DeathOp u8@+0x08 (pad 3B@+0x09..+0x0B); mode-sub-selector u32@+0x0C (meaning is mode-dependent); source/killer-actor key u32@+0x10 (looked up kind=3; only mode 0). **DeathOp/mode table:** {0=kill-setup: record killer + drive dying actor into killed-by visual state; 1=spawn death effect anchored on key@+0x04 with effect id = 350000038 + sub-selector@+0x0C (sub-selector 1..7 → ids 350000039..350000045); 2=set actor death sub-state at actor+0x594 to 6 when sub-selector@+0x0C==1, else 7; 3=revive/clear: actor back to alive/idle (action-state=1, death-sub-state=0, re-attach weapons, snap idle motion)}. Distinct from 5/10 (death notification) and 4/28/5/28 (respawn); part of the death-family Push set. Routing dispatch-table-confirmed; VALUE semantics of mode-sub-selector and actor keys capture-pending (R-CAP). |
| `0x50050` | SmsgPvpDeathFx | S2C | 16 | - | confirmed | (5:80) PvP death FX. ROLE (role confirmed on build 263bd994, Campaign 7): resolves a primary actor and an opponent actor, and drives PvP duel engage/disengage visual effects. **CYCLE 15 body layout (CONSUMER-CONFIRMED, FIXED 16 bytes):** leading dword@+0x00 not consumed (serial/echo); primary-actor key u32@+0x04 (lookup kind=1); gate flag u8@+0x08 (must be 1 for the effect-spawn step; both modes); pad u8@+0x09; DeathOp/mode u8@+0x0A; pad u8@+0x0B; opponent/source-actor key u32@+0x0C (lookup kind=3; also the effect anchor). **Mode table:** {1=PvP engage: gate@+0x08 must be 1; faces primary and opponent toward each other, sets motion-fx state, spawns persistent anchored aura 371003701 on opponent key; 6=PvP disengage: clears motion-fx state for both, deactivates effect 371003701 on opponent key, then (only if gate@+0x08==1) spawns burst 371003702 on opponent key; any other=no-op}. Effect 371003701 (mode 1 aura) is the same id mode 6 tears down — explicit engage/disengage pairing. Distinct from 5/10 (death notification) and 5/79 (actor death state); part of the death-family Push set. Routing dispatch-table-confirmed; gate flag and effect-id semantics CONSUMER-CONFIRMED (CYCLE 15); VALUE semantics of actor keys capture-pending (R-CAP). |
| `0x50055` | SmsgDungeonEventStateSyncA | S2C | var | - | confirmed | (5:85) Dungeon event state sync A. ROLE (role confirmed on build 263bd994, Campaign 7): reads a fixed block, fetches the dungeon/event-state object, and on its event-state values (two adjacent codes) arms a timer and writes state/flag fields on the local-player and a dungeon service slot, then commits the event-state object. **CYCLE 4 netcode-deep flags this as var->fixed; the precise wire frame size is not yet pinned in the inputs, so the size column stays `var` pending a frame-size confirmation.** Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x50056` | SmsgDungeonEventStateSyncB | S2C | 20 | - | confirmed | (5:86) Dungeon event state sync B. id/name/direction(S2C)/size(20) correct. **FIELD OFFSET CORRECTION (2026-06-24 byte-audit — YAML/catalog field offsets need spec-author update): fields are shifted +8 — Mode u8 @0x08 (NOT @0x00), ValueA u32 @0x0C, ValueB u32 @0x10; the leading 8 bytes are an opaque header with no Mode byte at offset 0.** ROLE (role confirmed on build 263bd994, Campaign 7): reads a fixed 20-byte block, fetches the dungeon/event-state object, and on a mode byte (0..4) arms a timer and writes state/flag fields on a per-event service slot and the local player, with two scenario branches keyed on a scenario id (300/301); commits the event-state object. **SIZE CORRECTED (2026-06-24 byte-audit): var→20 (fixed 20-byte handler read confirmed).** Routing + 20-byte read are control-flow-confirmed; field layout not yet specced. |
| `0x50057` | SmsgDungeonEventActorClear | S2C | var | - | confirmed | (5:87) Dungeon event actor state clear. ROLE (role confirmed on build 263bd994, Campaign 7): reads a fixed 16-byte block and forwards a cleared actor/state value into the actor manager. Size note: the 16-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x50058` | SmsgPvpStateBytes | S2C | var | - | confirmed | (5:88) PvP state bytes. ROLE (role confirmed on build 263bd994, Campaign 7): reads a small fixed block and forwards eight payload bytes into the PvP-state object. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x50059` | SmsgPvpRevengeRoster | S2C | 188 | - | confirmed | (5:89) PvP revenge roster. **SIZE CORRECTED (2026-06-24 byte-audit): var→188 (handler ReadField 0xBC; YAML body widths sum to 188).** ROLE (role confirmed on build 263bd994, Campaign 7): reads a fixed 188-byte roster snapshot block and copies it into the PvP state object. Routing + 188-byte read are control-flow-confirmed; field layout not yet specced. |
| `0x5005a` | SmsgPvpCounters | S2C | 32 | - | confirmed | (5:90) PvP counters. **SIZE CORRECTED (2026-06-24 byte-audit): var→32 (handler ReadField 0x20).** ROLE (role confirmed on build 263bd994, Campaign 7): reads a fixed 32-byte block and hands it to the PvP-state subsystem singleton. Routing + 32-byte read are control-flow-confirmed; field layout not yet specced. |
| `0x5005b` | SmsgPvpScoreUpdate | S2C | 124 | - | confirmed | (5:91) PvP score update. **SIZE CORRECTED (2026-06-24 byte-audit): var→124 (handler ReadField 0x7C).** ROLE (role confirmed on build 263bd994, Campaign 7): reads a fixed 124-byte block and forwards it to the PvP score/ranking subsystem. Routing + 124-byte read are control-flow-confirmed; field layout not yet specced. |
| `0x5005c` | SmsgPvpRequestOrNotice | S2C | 40 | - | confirmed | (5:92) PvP request / notice. **SIZE CORRECTED (2026-06-24 byte-audit): var→40 (handler ReadField 0x28).** ROLE (role confirmed on build 263bd994, Campaign 7): reads a fixed 40-byte block and forwards it to the PvP request/notice subsystem. Routing + 40-byte read are control-flow-confirmed; field layout not yet specced. |
| `0x5005d` | SmsgActorMobIdChange | S2C | 16 | - | confirmed | (5:93) Actor mob-id change. **SIZE PINNED to a FIXED 16-byte 4-field record (CYCLE 4 netcode-deep, the binary wins -- was "var"; field names recovered from the binary's own debug string).** ROLE (role confirmed on build 263bd994, Campaign 7): reads the 16-byte body and forwards it to the actor-manager apply routine (mob-id/template change -- polymorph/transform). Routing + the 16-byte read are control-flow-confirmed; field VALUE semantics not yet specced. **IDB label note (build f61f66a9, CYCLE 14): the live IDB MISLABELS this Push slot (minor 93) as SmsgVoteResult due to a naming collision in the names.yaml rebuild. The catalog name SmsgActorMobIdChange is confirmed correct by the handler's own binary debug string (logs actor_sort, actor_id, old_mob_id, new_mob_id). A corrective rename is pending (ida-toolsmith).** |
| `0x5005e` | SmsgVoteResult | S2C | 16 | - | confirmed | (5:94) Vote result. **SIZE PINNED to a FIXED 16 bytes (CYCLE 4 netcode-deep, the binary wins -- was "var").** ROLE (role confirmed on build 263bd994, Campaign 7): reads the 16-byte body and forwards it to the vote subsystem object for tally/update. Routing + the 16-byte read are control-flow-confirmed; field VALUE semantics not yet specced. **IDB label note (build f61f66a9, CYCLE 14): the live IDB labels this Push slot (minor 94) as SmsgVoteResult_0 (dedup suffix applied because another function displaced the un-suffixed label in the rebuild). The catalog name SmsgVoteResult is confirmed correct by the handler's binary debug string and MessageDB references 54098-54101. A rename dropping the _0 suffix is pending (ida-toolsmith).** |
| `0x50062` | SmsgActorClassFormRefresh | S2C | var | - | confirmed | (5:98) Actor class/form refresh. ROLE (role confirmed on build 263bd994, Campaign 7): reads a fixed block, primes the actor-lookup singleton, then forwards the block to the class/form apply routine that refreshes the actor's class/form visual. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x5006a` | SmsgTradeStateToggle | S2C | var | - | confirmed | (5:106) Trade state toggle. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 12-byte block, looks the actor up by (id,sort), and on a flag byte sets or clears the actor's trade-busy state; when the actor is the local player it toggles the trade-related UI panel, plays the open/close cues, and propagates the busy flag to a linked partner actor. Size note: the 12-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x50079` | SmsgCharPropertySet | S2C | 8 | packets/5-121_char_property_set.yaml | confirmed | (5:121) Character property set. ROLE (role confirmed on build 263bd994, Campaign 7): reads two values (a target key and a property value), looks up the actor, and writes the value into a per-actor property field when the lookup succeeds. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x5007b` | SmsgGiftCharReceiveConfirm | S2C | var | - | confirmed | (5:123) Gift character receive confirm. ROLE (role confirmed on build 263bd994, Campaign 7): clears a pending notice flag then reads a 16-byte block and forwards it to the gift/receive panel. Size note: the 16-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x5007c` | SmsgActorVisualFlagsSet | S2C | 12 | packets/5-124_actor_visual_flags_set.yaml | confirmed | (5:124) Actor visual flags set. **FIXED 12-byte payload (CONSUMER-CONFIRMED CYCLE 15):** sort u32@+0x00 (actor lookup sort/category), actor_id u32@+0x04 (actor lookup id), visual_flags u8@+0x08 (bitmask written verbatim to actor+0x378; local-player copy also mirrored to a client global; triggers full visual refresh + weapon-node visibility re-apply), pad 3B@+0x09. **Visual-flags bitmask (actor+0x378):** bit 0x04 = VF_PairedCompanion — when set, the actor's sort-15 companion sub-entity has its visual scale halved and motion reset/re-applied in the actor visual-refresh routine. All other bits present in the bitmask but unenumerated statically. Routing dispatch-table-confirmed; VALUE semantics of remaining bitmask bits capture-pending (R-CAP: observe actor+0x378 across known visual states such as weapon-drawn, mount, etc.). |
| `0x5007e` | SmsgActorStanceSet | S2C | 12 | packets/5-126_actor_stance_set.yaml | confirmed | (5:126) Actor stance set. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 12-byte body, resolves the actor by composite key, and writes a single stance/state byte onto the actor. Size note: the 12-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x5007f` | SmsgStealthToggle | S2C | 12 | packets/5-127_stealth_toggle.yaml | confirmed | (5:127) Stealth toggle. **FIXED 12-byte payload (CONSUMER-CONFIRMED CYCLE 15):** sort u32@+0x00 (actor lookup sort; low byte compared against Actor+0x60), actor_id u32@+0x04 (actor lookup id; matched against Actor+0x5C), stealth_flag u8@+0x08 (boolean stored as 0/1 to Actor+0x72C: nonzero=stealth ON, 0=stealth OFF), pad 3B@+0x09. **stealth ON (nonzero):** the client releases all of the target actor's visual nodes for remote viewers — actor disappears. **stealth OFF (zero):** the client rebuilds the actor's skin and re-uploads the deformed mesh — actor reappears. **Local-player exemption:** if the toggled actor's id matches the local player, the flag is stored at Actor+0x72C but the visual release/rebuild is skipped; a stealthed local player continues to see their own model; only remote observers' clients hide it. VALUE semantics CONSUMER-CONFIRMED (CYCLE 15); stealth_flag is a boolean (nonzero test, not bit extraction). Routing dispatch-table-confirmed. |
| `0x50081` | SmsgMonsterNoticeByMobId | S2C | var | - | confirmed | (5:129) Monster notice by mob id. ROLE (role confirmed on build 263bd994, Campaign 7): reads a mob id, resolves it to a monster table record, formats a notice string from a preset template, and posts it to the chat panel. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x50082` | SmsgMonsterNoticeWithText | S2C | var | - | confirmed | (5:130) Monster notice with text. ROLE (role confirmed on build 263bd994, Campaign 7): reads a fixed block carrying a mob id and a text region, looks up the mob template, formats a notice string from a string-table id, and posts it to the main message UI. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x50083` | SmsgPvpRankScoreUpdate | S2C | 8 | - | confirmed | (5:131) PvP rank score update. **SIZE CORRECTED (2026-06-24 byte-audit): var→8 (handler ReadField 0x08; two u32 dwords).** ROLE (role confirmed on build 263bd994, Campaign 7): reads two u32 dwords into the rank/progress state object's score fields and refreshes its panel. Routing + 8-byte read are control-flow-confirmed; field layout not yet specced. |
| `0x50088` | SmsgActorTimedStateUpdate | S2C | 16 | packets/5-136_actor_timed_state_update.yaml | confirmed | (5:136) Actor timed-state update. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 16-byte block, resolves the target actor by id (falling back to the actor manager lookup), and writes a timed-state value plus a one-byte state flag onto it (propagating to a linked actor), then pulses a UI element. Size note: the 16-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x5008b` | SmsgAttackEffect | S2C | var | - | confirmed | (5:139) Attack effect. ROLE (role confirmed on build 263bd994, Campaign 7): reads a 12-byte block, resolves the source actor and its effect template, and spawns the attack/impact visual effects and positional 3D SFX selected by effect-id ranges and an actor effect-kind code. Size note: the 12-byte read is the handler's bulk-read minimum, not a proven frame size. Routing dispatch-table-confirmed; field layout not yet specced. |
| `0x50092` | SmsgAckRequest | S2C | 8 | packets/5-146_ack_request.yaml | confirmed | (5:146) Server ack-request push for PvP zone presence detection. Fixed 8-byte body: Token u32 @0 (request token to be echoed back), ValidationValue u32 @4 (validated against local state). The client handler reads 8 bytes, validates the second word, shows message 51027 (hostile presence notice), and replies with C2S 2/146 carrying the token + local server id. Companion pair: C2S 2/146 CmsgServerAckEcho ↔ S2C 5/146 SmsgAckRequest. Routing + 8-byte read are control-flow-confirmed; field VALUE semantics capture-unverified. |
| `0x50093` | SmsgActorCombatFlagUpdate | S2C | 8 | - | confirmed | (5:147) Actor combat-flag update. **SIZE CORRECTED (2026-06-24 byte-audit): var→8 (handler does two single-u32 reads = 8 bytes).** ROLE (role confirmed on build 263bd994, Campaign 7): reads an 8-byte block, resolves the actor by id, and writes a combat-flag dword onto it; when the actor is the local player it mirrors the flag to a global and shows an on/off battle-mode notice. Routing + 8-byte read are control-flow-confirmed; field layout not yet specced. |

Status legend: `draft` (field layout hypothesized) - `observed` (seen in a capture) -
`confirmed` (cross-checked against the binary's dispatch table; here, opcode->handler routing is
confirmed even where the field layout is not yet specced) - `implemented` (C# struct + handler
exist).

> **Coverage gap — CLOSED (CYCLE 4 Netcode Deep-Cartography).** The earlier "~45 of ~90 listed,
> ~45 missing" note is **superseded**: CYCLE 4 promoted the **full major-2 C2S builder set** from the
> authoritative send-census (the single routine ~all C2S sends converge through) and the six W2 DTO
> dossiers. The binary's major-2 send surface is **96 distinct builders mapping onto 95 distinct
> `(2:minor)` opcodes** — the one collision is **2/52**, emitted by two builders (a short reactive
> form and a full player-driven form), which is why 96 builders → 95 rows. **All 95 distinct major-2
> opcodes now have an individual catalogue row above** (set-equal to the send-census major-2 list:
> **zero missing, zero extra, zero duplicate** — verified against the dirty-room send census and the
> completeness audit). The keepalive timer
> `2/10000` (armed once in the NetHandler ctor, not a dispatch-table builder) is catalogued separately.
> **The major-2 listing here is therefore the complete enumeration**, not a curated subset.
>
> Several minor numbers cited as "missing" in the prior note (e.g. 2/43, 2/51, 2/53, 2/59, 2/73,
> 2/77, 2/78, 2/96-2/98, 2/101-2/102, 2/104-2/105, 2/109, 2/111, 2/116-2/117, 2/119, 2/121, 2/125,
> 2/127, 2/129-2/133, 2/136, 2/138, 2/140, 2/144, 2/147-2/150) are **NOT real builders** — they do
> not appear in the send-census and have no send site in the binary, so they are correctly absent
> (there is nothing to catalogue for them). What remains open is **not** any un-mapped opcode but the
> wire **VALUE semantics** flagged `[capture/debugger-pending]` on the individual rows — the routing,
> send size, and field layout of every major-2 builder are control-flow-confirmed.

---

## Appendix A — Lobby protocol (login-server discovery; NOT a `(major:minor)` opcode)

The catalog table above is the **game connection** protocol. Before that connection exists, the
client talks to a **separate lobby / login-server surface** to discover which game server to join.
The lobby is **not** part of the `(major:minor)` family table and carries **no opcode dispatch** —
it is documented here so the `network-protocol-engineer` can build the login-server vs game-server
split. Full field spec: `packets/lobby.yaml`.

### Login-server vs game-server split

- **Lobby / login server** — a host:port resolved **locally** (see host resolution below), queried
  over a **synchronous, blocking** TCP socket. Two short-lived throwaway sockets, one per query.
- **Game server** — the host:port returned by the lobby's channel-endpoint query. The client then
  opens its **single persistent overlapped** game connection to it and runs the `(major:minor)`
  protocol above (0/0 → 1/4 → 3/1 → 1/9 → …). The lobby and game sockets never share a connection.

### The two lobby queries

| Query | Port | Response (after LZ4 decompress) |
|---|---|---|
| Server list | **10000** | An 8-byte frame wrapper (`+0 u32 size`, `+4 u16` reinterpreted as the server-entry **record count**, `+6 u16` unused), then that many **8-byte little-endian records** `{id/select-key i16 @+0, status/kind i16 @+2, population i16 @+4, flag i16 @+6}`. |
| Channel endpoint | **10000 + selected channel offset** | The same 8-byte wrapper, then a payload whose **first 30 (0x1E) bytes** are a fixed `char[30]` ASCII endpoint token naming the game server (consumed opaquely as one string). |

Both responses are **LZ4-compressed and carry NO byte cipher**, and there is **no `(major:minor)`
dispatch** on the lobby socket. The lobby wrapper's `+6 u16` (the game frame's "minor" word) is
unused on this path. The selected **channel offset** is added directly to 10000 to form the channel
query port, so it is effectively the channel **port offset**.

The wire carries only the numeric **id/select-key** (`+0`) plus a per-row caption message-id; the
localized server **display name** is **client-local** (a localized name table) and must be supplied
by a fresh implementation. The full per-entry field roles and the caption message-id mapping are in
`packets/lobby.yaml` (record shape A).

### Server-entry field roles + caption mapping (capture-unverified presentation)

- `+0 id/select-key`: the server id; (a) looks up the client-local display name, (b) draws the
  current-selection highlight when it matches the remembered "last server", (c) the value tested
  **`== 100`** is the **"available"** gate that unlocks the per-row select buttons (the 100 sentinel
  is on this `+0` field, NOT on a separate status field).
- `+2 status/kind`: caption/branch selector. `0` -> derive a population caption from `+4`/`+6`;
  `3` -> if `+4 == 24` caption message-id **6004**, else a latency caption **6005**; `1..39` ->
  index a per-value caption-string array (display name from `+0`); out of `1..39` -> fallback
  message-id **5901** ("unknown id"). Full enum is capture-only.
- `+4 population` + `+6 flag`: when `+2 == 0`, `+6` nonzero treats `+4` as a NUMERIC population
  (thresholds 1200/800/500 -> caption message-ids **6001/6002/6003**), `+6` zero treats `+4` as a
  DISCRETE load level (`+4 == 4/3/2` -> **6001/6002/6003**); `+6` is also reused as a `6005` latency
  numerator. The four column-header captions are message-ids **4029/4030/4031/4032**.
- Field signedness (`+0`/`+4`/`+6`) is presentation-only and capture-unverified.

### Lobby host resolution order (where the lobby host comes from)

The lobby connect helper resolves the lobby host in this strict priority order:

1. **`ip.txt`** in the working directory, if present — a single whitespace-free token, truncated to
   19 characters.
2. else a **`list.dat`** connection-IP-list record, selected by a registry server-name key.
3. else the hardcoded fallback host `211.196.150.4`.

This resolves the **lobby** host only; the **game** server host comes from the channel-endpoint query.

### `list.dat` record layout (loose client-root file, NOT in the VFS)

`list.dat` is a loose binary file in the client root. On-disk layout (file-size invariant
`filesize == 768 * count + 4`):

| Offset | Size | Field |
|---|---|---|
| +0 | u32 | record `count` |
| +4 | `count × 768` | records |

Per 768-byte record:

| Sub-offset | Field |
|---|---|
| +0 | CP949 server **name** — the match key, compared against the registry `servername` value (a default CP949 name baked in the client is used if the registry read fails) |
| +256 (0x100) | the lobby **host** string (IPv4 dotted) the client connects to |

The 256-byte gap between the name and +256, and whether +256 also holds a port, are unverified (no
on-disk sample) — see the open questions in `packets/lobby.yaml`.
