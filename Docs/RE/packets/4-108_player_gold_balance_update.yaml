# Clean-room field spec. Promoted from a neutral analyst note; rewritten, not copied.
#
# VERIFICATION BANNER
#   verification: confirmed  (opcode->handler routing and the fixed 16-byte (0x10) body -- Header[8]@0,
#                 Gold i64 spanning +0x08..+0x0F passed as low/high dwords to the player gold setter --
#                 are control-flow-confirmed)
#   ida_reverified: 2026-06-24
#   ida_anchor: 263bd994
#   ida_reverified: 2026-06-27 (CYCLE 14 deep-verify)   ida_anchor: f61f66a9   evidence: [static-ida]
#     -- CYCLE 14 deep-verify (f61f66a9): body layout re-traced against handler, 1 field re-confirmed SAME
#        (Gold i64@8, low@8/high@0xc), 0 corrected.
#   conflicts: none.

name: SmsgPlayerGoldBalanceUpdate
opcode: 0x4006c           # major 4, minor 108
direction: S2C
size: 16                  # fixed 16-byte body
spec: packets/4-108_player_gold_balance_update.yaml
status: confirmed
capture_verified: false

summary: Updates the local player's gold balance directly and refreshes money HUD labels.

fields:
  - { name: Header,      type: "bytes[8]" }   # 0x00 Opaque header/echo region
  - { name: Gold,        type: i64 }          # 0x08 New gold balance (64-bit integer)

notes: |
  The handler reads a fixed 16-byte body. Frame = 8-byte header + 16 = 24 bytes on the wire.
  Gold is represented as a 64-bit integer spanning offsets 0x08 to 0x0F.
