# RETIRED — this file is a superseded duplicate of opcode 2/19.
# CYCLE 14 re-anchor (2026-06-27, build f61f66a9): ida_anchor f61f66a9 (prior 263bd994); opcode dispatch confirmed structurally intact (specs/network_dispatch.md); body fields clean-relocation confirmatory.
# verification: idb-sha256-263bd994c927c20a38624cf0ca452eaef365057fa9db1543d8f668c14a6fd8ee
#
# AUDIT VERDICT (lane-02 packet audit): the binary stager packs four distinct single-byte
# selector fields at body @0x04-0x07 (Slot/SubSel/FlagA/FlagB) plus u32 Quantity @0x08,
# refuting the Field1 u32@4 / Field2 u32@8 model in this file.  Two committed YAMLs shared
# opcode key 0x20013; only one is authoritative.  The canonical spec for opcode 2/19 is:
#
#   packets/2-19_npc_buy_or_acquire.yaml  (status: verified)
#
# This file (CmsgNpcInteract / 12-byte generic-interact model) is retired and must NOT be
# loaded as an opcode spec.  It is kept only as a historical record of an earlier hypothesis.
# The active 2/19 row in opcodes.md is CmsgNpcBuyOrAcquire.

name: CmsgNpcInteract_RETIRED
opcode: ~                 # opcode intentionally cleared — see 2-19_npc_buy_or_acquire.yaml
direction: C2S
status: retired
spec: packets/2-19_npc_interact.yaml

notes: |
  Retired by lane-02 packet audit. The earlier 12-byte Field1/Field2 model for opcode 2/19 is
  refuted by the binary: the real layout has four single-byte selector fields at @0x04-0x07 and
  a u32 Quantity at @0x08. See packets/2-19_npc_buy_or_acquire.yaml for the verified spec.
