# Clean-room field spec. Promoted from a neutral analyst note; rewritten, not copied.
# ROLE CORRECTION (lane-07 audit): sole caller is CubeGamble_ClearAndSubmit (Cube-Gamble minigame
# panel). Body layout is 56-byte block A (panel+2140) + 16-byte block B (panel+2196) + 1-byte
# local-player flag + 3 pad. No material slots, no NPC actor index. Both prior readings
# (CmsgCraftSubmit / CmsgCharStatusSubmit) are refuted for the field split; size 76 and direction
# C2S are confirmed. Proposed rename: CmsgCubeGambleSubmit.
# The inner field layout of block A (56B) and block B (16B) is pending re-struct-analyst.
# capture_verified: false.
#
# verification: sha256:263bd994c927c20a38624cf0ca452eaef365057fa9db1543d8f668c14a6fd8ee

name: CmsgCubeGambleSubmit
opcode: 0x2008d           # major 2 (GameAction), minor 141 -> (2 << 16) | 141
direction: C2S
size: 76                  # fixed 76-byte payload (56 + 16 + 1 + 3)
spec: packets/2-141_craft_submit.yaml
status: confirmed
capture_verified: false

summary: >
  Cube-Gamble minigame submission, sent when CubeGamble_ClearAndSubmit fires. Carries a 56-byte panel
  data block A (panel+2140 region), a 16-byte panel data block B (panel+2196 region), a 1-byte
  local-player flag, and 3 bytes of padding. The inner field layout of block A and block B is pending
  re-struct-analyst work. Size 76 and direction C2S are binary-confirmed. The prior readings as a
  crafting recipe submission (CmsgCraftSubmit) and as a character-status panel submit (CmsgCharStatusSubmit)
  are both refuted: no material slots and no NPC actor index are present.

# Wire order, offset 0 = first payload byte. Pack=1; the widths sum to size: 76.
# Binary-accurate outer layout per CubeGamble_ClearAndSubmit builder (lane-07 audit).
# Inner field splits for BlockA and BlockB are pending re-struct-analyst.
fields:
  - { name: BlockA,          type: "u8[56]" }   # 0x00  56 bytes blitted from panel+2140 region [inner layout pending]
  - { name: BlockB,          type: "u8[16]" }   # 0x38  16 bytes blitted from panel+2196 region [inner layout pending]
  - { name: LocalPlayerFlag, type: u8 }          # 0x48  local-player flag (1-byte; source is a local state field)
  - { name: Pad49,           type: "u8[3]" }    # 0x49  padding to 76 bytes

notes: |
  CONTROL-FLOW CONFIRMED body size (76 bytes). Sole builder/caller: CubeGamble_ClearAndSubmit.
  Role and field-split corrected per lane-07 audit.

    0x00 (56B) BlockA          : opaque 56-byte block blitted from the Cube-Gamble panel state
                                  at panel+2140. Inner field layout: pending re-struct-analyst.
    0x38 (16B) BlockB          : opaque 16-byte block blitted from the Cube-Gamble panel state
                                  at panel+2196. Inner field layout: pending re-struct-analyst.
    0x48 (1B)  LocalPlayerFlag : a 1-byte flag sourced from a local player state field; exact
                                  semantic meaning is capture/debugger-pending.
    0x49 (3B)  Pad49           : padding to bring the total to 76 bytes.

  CONTEXT: fires from the Cube-Gamble minigame panel's clear-and-submit action. The 56+16+1+3 outer
  block split is binary-accurate; individual field decomposition within block A and block B requires
  a re-struct-analyst pass.

  PRIOR READINGS (refuted):
    CmsgCraftSubmit  : u32[14] material slots + u32[4] extra params + u8 NPC actor index — no
                       crafting NPC caller exists for this opcode; refuted.
    CmsgCharStatusSubmit : character status/stat-panel snapshot — also refuted by caller identity;
                           the 56+16+1 outer split is shared but the role attribution is wrong.

  PROPOSED RENAME: CmsgCubeGambleSubmit. Opcode catalog entry 0x2008d carries a name + field
  description drift candidate; see opcodesCatalogCorrections from lane-07 audit.

  UNKNOWNS to resolve (re-struct-analyst + capture):
    - inner field layout of BlockA (56 bytes, panel+2140 region);
    - inner field layout of BlockB (16 bytes, panel+2196 region);
    - the LocalPlayerFlag semantics;
    - the server-side response opcode.

  UNBLOCKS: Client.Application Cube-Gamble minigame submission path (76-byte fixed C2S).
