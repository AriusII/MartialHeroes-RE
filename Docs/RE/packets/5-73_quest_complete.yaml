# DEPRECATED / REDIRECT -- do NOT implement from this file.
#
# BINARY-WINS CORRECTION (HUD-II Wave 4): opcode 5/73 was previously catalogued here as
# SmsgQuestComplete (a "quest turn-in verdict"). A direct binary re-confirmation this wave proved this
# is WRONG: the 5/73 handler reads a 344-byte (0x158) block and forwards it to the GuildWarInfoPanel
# (the guild-war info window), drives its war-alarm banner, and rebuilds the panel. It is NOT a quest
# channel. The quest result returns on 5/68 SmsgQuestList ONLY.
#
# The correct, current spec for opcode 5/73 lives at:
#   packets/5-73_guildwar_info_update.yaml   (name: SmsgGuildWarInfoUpdate)
#
# This file is retained as a deprecation pointer so old citations resolve. It carries NO authoritative
# fields. There is NO separate "quest complete" opcode -- quest turn-in results ride 5/68.

name: SmsgGuildWarInfoUpdate
opcode: 0x50049           # major 5 (Push), minor 73 -> (5 << 16) | 73
direction: S2C
size: 344
spec: packets/5-73_guildwar_info_update.yaml
status: deprecated-redirect
capture_verified: false

summary: DEPRECATED redirect. 5/73 is SmsgGuildWarInfoUpdate; see packets/5-73_guildwar_info_update.yaml. The former "SmsgQuestComplete" reading is refuted -- quest results ride 5/68.

fields:
  - { name: Payload, type: "bytes[344]" }   # 0x000  see packets/5-73_guildwar_info_update.yaml

notes: |
  Do not implement from this file. Use packets/5-73_guildwar_info_update.yaml. The "quest complete"
  attribution for 5/73 is a binary-refuted mislabel; the quest result channel is 5/68.
