# SUPERSEDED / REDIRECT -- do NOT implement from this file.
# CYCLE 14 deep-verify (2026-06-27, build f61f66a9): ida_anchor f61f66a9; redirect confirmed intact --
# no guild-war handler exists at 5/73; opcode routes to SmsgQuestComplete (5-73_quest_complete.yaml),
# re-confirmed SAME, 0 corrected.
#
# BINARY-WINS CORRECTION (CYCLE 4 netcode-deep): opcode 5/73 was catalogued here as
# SmsgGuildWarInfoUpdate. A fresh instruction-level reading of the 5/73 handler refutes that: the
# handler gates on a quest verdict, plays a quest-completion cue (success/cancel), and forwards the
# 344-byte block to a quest-RESULT panel that lays out a 10-row objective/reward list (CP949 names +
# integer counts). It references NO guild manager, NO guild-war string, and NO war-window call. The
# 344-byte size and the verdict header (gate @+0x08, subtype @+0x0C) are identical to the prior reading;
# only the NAME/role changes -- 5/73 is SmsgQuestComplete, not a guild-war info window. THE BINARY WINS.
#
# The authoritative, current spec for opcode 5/73 lives at:
#   packets/5-73_quest_complete.yaml   (name: SmsgQuestComplete)
#
# This file is retained ONLY as a redirect pointer so old citations resolve. It carries NO authoritative
# fields. There is no guild-war info-window opcode wired at 5/73.

name: SmsgQuestComplete
opcode: 0x50049           # major 5 (Push), minor 73 -> (5 << 16) | 73
direction: S2C
size: 344
spec: packets/5-73_quest_complete.yaml
status: superseded-redirect
capture_verified: false

summary: SUPERSEDED redirect. 5/73 is SmsgQuestComplete; see packets/5-73_quest_complete.yaml. The former "SmsgGuildWarInfoUpdate" reading is binary-refuted -- the handler drives a quest-result panel, not a guild-war window.

fields:
  - { name: Payload, type: "bytes[344]" }   # 0x000  see packets/5-73_quest_complete.yaml

notes: |
  Do not implement from this file. Use packets/5-73_quest_complete.yaml (SmsgQuestComplete). The
  guild-war info-window attribution for 5/73 is a binary-refuted mislabel. NOTE: the guild-relations
  declare round-trip is a SEPARATE mechanism -- C2S 2/81 CmsgGuildDiplomacyDeclare -> S2C 4/61
  SmsgGuildStateChangeResult (packets/4-61_guild_state_change_result.yaml); the generic action-error
  result is 4/81 SmsgActionErrorResult. None of those is 5/73.
