<Project Sdk="Microsoft.NET.Sdk">

    <!--
        skillcat-scan — THROWAWAY harness for the icon-SOURCE-trace mission.
        Reads data/script/skillcategory.scr (564-byte records) and recovers
        the per-skill (iconSrcX, iconSrcY) array proven by IDA at record+0x220.
        NOT a MartialHeroes.slnx member. Run with dotnet run -c Release.
        Do not git add this file or any bin/obj output.
    -->

    <PropertyGroup>
        <OutputType>Exe</OutputType>
        <TargetFramework>net10.0</TargetFramework>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
        <LangVersion>14.0</LangVersion>
        <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
        <IsPackable>false</IsPackable>
    </PropertyGroup>

    <ItemGroup>
        <ProjectReference Include="C:\Users\Arius\RiderProjects\MartialHeroes\03.Storage.Assets\MartialHeroes.Assets.Vfs\MartialHeroes.Assets.Vfs.csproj" />
    </ItemGroup>

</Project>
